# Phase 1 acceptance: repeatable desktop journeys

> Archived reference, 9 September 2026. Preserved for evidence and migration; instructions and unchecked lists below are historical, not the current product plan. See [Product](../../../PRODUCT.md) and [Roadmap](../../../ROADMAP.md).

Status: maintained acceptance contract and test plan, updated 2026-09-06. This is the single testing guide for Phases 1 and 2. Vitest, checked-in desktop Playwright journeys, an opt-in real-Pi casebook and a local testing dashboard run today. Automated eval checks do not imply human-reviewed model quality or complete Phase 1 acceptance. All executable testing code, runner configurations and generated reports live under [tests/](../../../tests/README.md).

This guide owns acceptance cases, test/eval procedures, and dated verification evidence. The [journey](../../user-journeys/01-application-launch.md) owns expected product behavior; the [roadmap](../../../ROADMAP.md) owns development order and implementation status. Failures found here become linked roadmap work, not a competing build plan.

## What “done” means

Acceptance is observable evidence that an engineer can complete a workflow and recover from its expected failures. It is not just a green test count or a model saying that work is complete.

There are two different completion claims:

- **An application's Launch Brief is ready:** its four current checks pass: recorded identity, readable repository at a recorded commit, explicit target environment, and explicit permission policy. This does not mean the application is deployed.
- **Phase 1 product work is complete:** the current journey below passes, plus the explicit GitHub connection and durable-request follow-ups in the [retained acceptance record](../previous-direction/docs/plans/implementation-history.md#phase-1-implementation-backlog) pass their acceptance cases. GitHub connection has automated and live happy-path evidence; durable requests have synthetic process and browser coverage, while live model verification and broader acceptance work remain. Do not treat the four checks as evidence that the entire product phase is done.

Desktop only. Do not add mobile checks unless the user changes the project scope.

## Current entry points and setup rules

| Entry | Expected behavior |
| --- | --- |
| `/` | Redirects to `/applications`, never automatically selects the newest application. |
| `/applications` | Lists application/repository identity, target and passing-check count; empty state offers Add application. |
| `/applications/new` | Blank HTTPS/SSH GitHub repository input and explicit permission policy, initially Pi decides. A round trip to GitHub settings preserves both fields in this browser tab. Shows the selected GitHub account, or a Connect GitHub action; submission is disabled until a connection is chosen. Success opens the returned workspace. |
| `/applications/[applicationId]` | Selected application's chats/checks; `?chat=…` preserves the selected transcript on refresh. The application-name menu provides switching, adding and confirmed removal. Unknown or removed application identity returns 404, never another app. |
| `/setup/pi` | Settings: account first, model/effort, then View applications. Changed preferences save before navigation. Storage & privacy opens technical details. |
| `/setup/github` | Automatically identifies reusable GitHub credentials, requires consent to reuse, or starts a separate GitHub App device login. Shows account, source, expiry/recovery, repository-installation link for App connections and disconnect. |
| `/setup/execution` | The controller host's Docker Engine as last checked: state, machine, one-sentence reason, one recovery action with official instructions, Check again, Prepare execution environment with visible progress, technical details. Never installs or changes anything. |

Pi and GitHub connections are installation-wide; chats, Decisions, Observations and checks are application-scoped. Switching applications clears transient drafts, drawers, errors and busy state. Production is a target, not evidence of deployment. Prototype A is the real setup screen; no variant parameter or demo controls are required.

Reuse must import the detected model/effort with explicit consent. Separate sign-in preserves the draft selection and writes a new attempt-specific credential file. Only successful login activates it by atomically replacing Server Guy's configuration pointer; cancellation/failure leaves the old login and preferences intact. Existing accepted credentials remain available to in-flight turns. Global Pi model preferences are never overwritten; unrelated tools/extensions/instructions are never imported. Reading saved credentials does not prove live provider access.

Technical help must accurately explain credential paths, shared versus separate storage, owner-only permissions for new files, lack of application-level token encryption, and the distinction between the server machine and the browser device. Keep these details out of repeated main-card copy.

## Test layers and tooling

| Layer | Tool | What it proves |
| --- | --- | --- |
| Schemas, domain rules, transactions, login coordination | Existing Vitest | Deterministic rules, isolation, atomic writes, cancellation and failure handling. |
| Browser journey through real routes and SQLite | `npm run test:e2e`, Playwright Test | Forms, navigation, dialogs, visible errors, keyboard behavior, reload and recovery. External Pi/GitHub calls use deterministic fixtures. |
| Real Pi behavior | Opt-in `npm run eval:pi`, using existing Vitest and the actual Pi adapter | Exact proposal/state assertions plus a separate human meaning review. A synthetic fixture cannot prove real model behavior. |
| Meaning of a saved Server Guy answer | Human review; optional opt-in LLM judge | Rubric-based semantic review. LLM advice never changes automatic results, application state, or human sign-off. |

Playwright provides [fixtures](https://playwright.dev/docs/test-fixtures), isolated [browser contexts](https://playwright.dev/docs/browser-contexts), and [failure traces](https://playwright.dev/docs/trace-viewer). Next.js also recommends E2E testing for [async Server Components](https://nextjs.org/docs/app/guides/testing). This fits our server-rendered entry pages and multi-screen workflow.

Use Vitest + Playwright Test with the small casebook, not an additional evaluation platform or workflow engine. The local dashboard only launches fixed commands and reads/writes private result artifacts. It is not part of the production Next.js application.

## Current journey contract

Each case needs assertions about both what the engineer sees and the resulting state. Expected errors are successful tests only when recovery and unchanged data are also checked.

| ID | Steps | Required result |
| --- | --- | --- |
| P1-01 Fresh setup | Open Settings without Server Guy configuration, with and without a reusable Pi login; repeat with broken saved configuration or missing credentials. | Discovery is automatic and read-only. Reuse requires explicit consent; otherwise Connect ChatGPT is offered. No OAuth access/refresh tokens appear in page data (a device code during explicit sign-in is intentional). Chat stays unavailable until setup succeeds. |
| P1-02 Account setup | Complete, cancel, reject, and expire device-code login; repeat when replacing a saved connection. | Success activates the chosen login. Cancel/failure preserves the old connection. Late completion cannot revive a cancelled attempt. Retryable errors leave recovery controls available. |
| P1-03 Preferences | Choose a supported model/effort, save, reload, and send. Try unavailable model and quota/auth failures. | Preferences persist only in Server Guy. Unsupported choices are rejected; provider failures are visible and never silently change provider or billing mode. “Login saved” is not a claim that provider access was tested. |
| P1-04 Add application | Home → Applications → Add application. Supply HTTPS/SSH repository and a permission policy. Try invalid URL, unreadable repository and duplicate with a conflicting policy. | Valid creation opens the exact new workspace. Invalid input remains editable. Failed access is represented by a failed check, not a false pass. Policy conflict preserves the existing record. Creation does not deploy or modify the repository. |
| P1-05 Isolation | Add two applications; send a distinct message/Decision in each; switch, reload, and open a second chat. | Application records never leak across applications. Chats share application Decisions but retain separate transcripts. Switching clears unsent composer text and open drawers. |
| P1-06 Decisions | State an explicit launch priority; revise it using an existing Decision; also send an ordinary greeting. | Explicit priority/revision has the correct source-message provenance and supersession. Greeting creates no invented Decision. Visible reply agrees with accepted machine-readable proposals. |
| P1-07 Failure atomicity | Trigger provider failure, malformed proposal, unsupported kind, or fabricated replacement ID; retry when appropriate. | Accepted user intent survives. An unsuccessful assistant attempt stays visibly unfinished and saves no Decisions. Final answer, Decisions and successful Pi Run commit together only after validation. Retry creates a linked assistant attempt, not another user message. |
| P1-08 Evidence | Inspect repository check details; fail inspection, then rerun successfully. Inspect other record tabs and reopen the page. | Check status follows stored evidence, with source/identity/time inspectable. A newer failed check must not leave an obsolete pass in force. Pending later-phase requirements are not presented as completed deployment. |
| P1-09 Fresh application state | App-name menu → Remove application. Cancel, try wrong confirmation, then confirm the exact owner/name on disposable data. Add that repository again. | Cancel/wrong confirmation changes nothing. Only that application's records are removed; other apps and login are preserved. Re-adding gets new IDs and no old chats/Decisions. Late results tied to old IDs cannot refill the fresh application. |
| P1-10 Disconnect | Settings → Disconnect. Cancel, then confirm on disposable configuration; reload and attempt a new message. | New Pi turns require setup again. All applications/chats and credential files remain. Pending login attempts are cancelled. Read-only discovery may still find a login but cannot adopt it automatically. An already-running turn may finish. |
| P1-11 Desktop usability | Complete the main path with keyboard; open help/menu/dialog, Escape/Cancel, and inspect busy/error states. | Named controls, visible focus, modal focus containment, sensible focus restoration, no unintended submission or navigation, no unexpected console errors. Storage details are behind Storage & privacy, not repeated prose on the form. |
| P1-12 Chat lifecycle | Create a secondary chat, switch between unsent drafts, send, archive, reopen and reload it; inspect the primary chat. | Drafts stay separate during the page session. Refresh and repository rechecks keep the selected transcript. Archived chat stays readable, explicitly says read-only and cannot accept messages. The primary chat has no archive action. History and shared Decisions remain intact. Unsent chat drafts are not durable across reload. |
| P1-13 Slow actions and navigation | Double-press Enter during a slow send; type a new draft while the response is pending. Start slow repository creation and leave the form. | One submission produces one user/assistant pair. New draft survives completion. Leaving creation is navigation, not cancellation: creation may finish, but must not redirect the engineer away from their new location. |
| P1-14 Check drawer and record tabs | Open each check; navigate Record/Activity/Changes/Receipts with mouse and arrows/Home/End; close via X, Escape and backdrop; choose Ask Pi, including from an archived chat. | Correct evidence and history; no invented external changes. Tabs expose their selected panel and keyboard focus. Modal contains focus and restores it on close. Ask Pi closes the drawer, prefills and focuses the composer; from archived history it opens the active main chat first. |
| P1-15 Login transport and late callbacks | Interrupt an attempt-status poll then restore it; return 404 for a missing attempt; cancel before a late callback; simulate SDK post-write synchronization failure. | Transient failure retries with Cancel available; 404 is terminal. Late callbacks cannot revive cancellation. A saved login plus local sync failure is distinguished from failed OAuth, without serializing the credential-bearing error. |
| P1-16 Model/runtime boundary | Reuse non-default supported model/effort; change away from a Max-capable model; tamper with saved provider/effort/API credentials; inspect already-running versus new turn configuration. | Reuse uses detected settings, invalid combinations are rejected or reset visibly, and new turns use saved preferences while in-flight turns keep their snapshot. API-key/other-provider fallback is rejected before provider authentication. |
| P1-17 GitHub connection | Detect/reuse a login; complete/cancel/deny/expire separate App device login; disconnect and reconnect; change/revoke the credential. | Detection alone grants no application access. Tokens never reach browser data/evidence. Replacement activates only on success; late callbacks cannot undo cancellation or disconnect. History survives. Both login paths automatically recheck existing applications, showing progress and per-repository results. Failed checks do not loop on reload; manual retry remains available. Stale or concurrent automatic requests cannot duplicate or overwrite new-connection evidence. |
| P1-18 Exact GitHub access | Check a selected private repository; deny scope, suspend/uninstall the App or omit that repository; retry after fixing access. Return a changed account/repository ID or a different repository at the same URL. | Only the exact accessible repository/commit with the current connection can pass. Safe evidence distinguishes the user's repository role from the App's permissions. Failure stays visible and cannot inherit a stale pass. No repository writes. |
| P1-19 GitHub renewal | Sign in to the App, expire only its access token in an isolated fixture, then re-run a repository check. Also reject/expire refresh, interrupt the network, race concurrent requests and disconnect/replace during renewal. | Successful renewal rotates both tokens once, retains the connection ID and needs no sign-in. Transient failure is retryable; rejected/expired renewal requires sign-in. Late callbacks cannot restore a disconnected/replaced login or invalidate a newer token. Neither token reaches browser data or evidence. Old-format logins explain their one-time new sign-in. |

| P1-20 Durable replies | Submit twice with one request key; leave/reopen the chat; cancel and retry; crash/restart the worker; queue another Chat; compact history; start a new chat after native history is lost. | Acceptance returns HTTP 202 with stable IDs. One worker serializes requests and retains writer locks until SDK settlement. Reconnect reads saved revisions; failure/cancellation saves no Decisions. A restart marks running attempts interrupted, never silently repeated. Native compaction preserves the conversation branch; scoped lookups read current Decisions. Missing history never silently starts an empty session or retries. Starting a new chat leaves existing records untouched. |

The Phase 1 Record sidebar shows only current checks and Decisions. Later server/domain prerequisites remain specification/planning data, not reminders or blockers in this view.

P1-06 has two layers: deterministic fixture tests prove persistence/UI plumbing; live Pi cases prove model behavior. Do not substitute one for the other.

## Reset and disconnect: deliberately separate

**Start an application from scratch:** open its name in the workspace header, choose **Remove application…**, type the displayed `owner/name`, and confirm. Removal is permanent; it is not chat archival and has no undo. Re-add the same repository in the form that opens afterward. Its GitHub repository, all other applications and installation-wide login are untouched.

**Disconnect ChatGPT from Server Guy:** open **Settings → Disconnect**. This removes `pi-settings.json` (the saved consent/connection/model selection) and cancels pending sign-in attempts. It does **not** delete accepted OAuth files, revoke provider tokens, sign out ChatGPT/Pi, or clear application records. Shared Pi login files must never be deleted by this control. Reconnecting requires another explicit choice.

**Disconnect GitHub from Server Guy:** open **Settings → GitHub → Disconnect**. This cancels its pending sign-in and replaces the owned `github-connection.json` with `null`, removing its current saved token/selection. GitHub CLI/environment credentials and application history are untouched. Upstream authorization is revoked separately on GitHub. A newly selected connection requires a fresh repository check.

No “nuke everything” browser endpoint is needed. For repeated automated runs, use isolated temporary databases/configuration instead of clearing the user's working installation.

## Run today's checks

The easiest entry point is:

```sh
npm run test:dashboard
```

Open **http://127.0.0.1:4317**. Nothing runs automatically. **Run checks** shows each suite's scope, model usage, CI policy, recent runs and bounded logs. **Review live eval answers** opens existing saved evals without rerunning Server Guy. **How it works** compares the four suites in one table and holds the file index. The page binds to loopback only, rejects foreign Host/Origin requests, requires same-origin JSON and a per-process token for controls, and runs one allowlisted command at a time. Do not expose it through a tunnel. Live SDK logs are deliberately not retained because they can contain sensitive diagnostics.

CLI alternatives, from the repository:

```sh
npm test
npx tsc --noEmit
npm run lint
npm run test:e2e:smoke
npm run test:e2e
# Interactive runner UIs:
npm run test:ui
npm run test:e2e:ui
```

Install Chromium once after `npm ci`: `npx playwright install chromium`. On Linux CI use `npx playwright install --with-deps chromium`. The dashboard and runner UIs require no model calls just to open or inspect saved output.

Start a disposable desktop fixture in another terminal:

```sh
node tests/browser/qa-fixture.mjs 3112 success fresh
```

Open `http://127.0.0.1:3112/`. Each invocation creates a new temporary app copy, SQLite database and Pi/config directories. It does not copy `.server-guy`, `.env` or user credentials. The startup output names those directories. Stop with Ctrl+C; the fixture stops Next.js and deletes its temporary files. Only a hard kill can leave scratch state behind. Use another free port from 3100–3999 for a separate run, never the user's port 3000.

Options: `success`/`failure` chooses synthetic ChatGPT OAuth outcome; `fresh`/`ready` chooses starting configuration. ChatGPT OAuth routes are replaced; real Pi coordinator races are covered by Vitest. GitHub uses its real routes/coordinator and synthetic API/device/credential responses; edit the fixture's `state/github-scenario.json` for `login: pending|denied|expired|success`, `credential: missing|changed`, or `repository: missing-scope|denied|revoked`. Omitted fields use the successful fixture. No real accounts are contacted.

Useful fixture inputs:

| Input | Scenario |
| --- | --- |
| `https://github.com/qa/example` | Synthetic readable repository; no GitHub write. |
| Repository name containing `missing` or `offline` | Access failure or network failure. For recoverable permission denial, set `repository: missing-scope` in the fixture scenario, then clear it and re-run the check. |
| Repository name containing `slow-create` | Slow creation; leaving the form must not trigger late navigation. |
| `priority: Fast recovery matters most` | A synthetic typed Decision proposal. |
| `replace-priority: Simplicity matters most` | A synthetic replacement proposal. |
| `invalid-replacement: test` | Invalid replacement must reject the turn. |
| Message containing `[fail-once]` or `[slow]` | Recoverable provider error or a delayed turn. |

The synthetic Pi adapter bypasses real provider authentication and generation. Use real-adapter Vitest tests for configuration enforcement; use opt-in live checks for actual provider behavior. Browser success alone is not enough.

## How to run an agent audit

Split independent agents by account/setup (P1-01–03, P1-10–11, P1-15–16), application/chat journeys (P1-04–09, P1-11–14), and server/transaction invariants (cross-check both). Each browser agent owns its own fixture port, server/config/database and tabs; never reset shared port 3000, read real credentials, call live providers, or run schema push against the user's database.

Report each case as **passed**, **failed**, **partial**, **blocked**, or **not run**, with concrete steps, expected/observed result, source locations where relevant, and the evidence layer: browser, HTTP, unit test, source-only, or live provider. Capture and inspect screenshots for meaningful UI states/findings. A fixture limitation is not a product failure; a mocked success is not evidence of provider correctness. Do not inherit a pass from an old report. Keep the latest run summary here and detailed disposable evidence outside this maintained docs folder.

This workflow authorizes audit/test work, not unrelated product fixes or new dependencies. Record reproducible defects for the next implementation pass. Automated browser submission of destructive actions must follow the active tool's confirmation policy; if blocked, test the UI up to confirmation and report API/domain coverage separately.

## Desktop automation and CI policy

The [shared catalog](../../../tests/browser/journeys.ts) contains fifteen selectable groups, including [GitHub connection](../../../tests/browser/github.spec.ts), durable requests, [native history/recovery](../../../tests/browser/native-sessions.spec.ts), the [Phase 2 Application Contract](../../../tests/browser/phase-two.spec.ts) and dashboard testing. One group can contain several tests; see the dated verification below for the tested case count. These cover selected contract branches, not every branch of the acceptance cases. Further fresh Pi setup/device-code variants, evidence/chat-lifecycle paths and the complete keyboard audit remain broader acceptance work.

The dashboard's **Choose journeys** dialog describes each scenario and runs only the selected stable tags from [the shared catalog](../../../tests/browser/journeys.ts), without scrolling away from the suite table. Empty, duplicate and unknown selections are rejected. Browser smoke remains the two tagged smoke journeys; selecting a local subset does not change CI.

- One Chromium worker, desktop 1440 × 1000, no automatic test retries. Each application-suite worker owns a disposable app/database/configuration; scenarios use distinct repository identities and restore synthetic model preferences. Contexts isolate browser state; they do not by themselves isolate SQLite.
- Tests assert UI behavior and saved state: priority provenance, exact replacement, provider retry recovery, application isolation, typed removal confirmation, disconnect/reuse and double-send handling. Accessible labels/roles are preferred over coordinates or generated-prose snapshots.
- Reports live in `tests/results/browser-report/`; traces and screenshots in `tests/results/browser-artifacts/`. Open a saved report with `npx playwright show-report tests/results/browser-report`. Do not upload real OAuth codes/tokens, account details, or private transcripts.

| Trigger | Checks | Model usage |
| --- | --- | --- |
| Every PR / push to main | Vitest, lint, TypeScript, production build, **two** browser smoke journeys | None; GitHub-hosted runners consume Actions minutes, not Codex/Pi credits. |
| Manual GitHub Actions run with full suite selected | Above plus all desktop scenarios instead of smoke | None; synthetic adapters only. |
| Local explicit start | Selected real Server Guy agent eval cases or selected saved-answer LLM judgments | Uses configured ChatGPT subscription; never silently falls back to API billing. |

The [workflow](../../../.github/workflows/checks.yml) uploads only synthetic browser failure artifacts, retained seven days. Live evals and judge results are never uploaded by this workflow. Workflow configuration is not proof of a successful hosted CI run.

## Real Pi casebook and remaining Phase 1 gates

The runnable [casebook](../../../tests/evals/phase-one-cases.ts) contains thirty-seven fixed inputs and meaning rubrics: the original eight Decision/instruction cases, three GitHub evidence cases, four defaults-and-requirements cases, seven application status cases, seven native conversation cases, and eight Application Contract cases (Phase 2: conforming, missing health, localhost bind, SQLite contradiction, malicious README, unsupported Django, user correction, stale commit) that seed a Phase 2 workspace with a synthetic inspection and saved reads so no GitHub request is made. The native cases cover old active records, cross-chat supersession, correction versus addition, implicit constraints and cancellation before/after compaction. They seed synthetic earlier history/usage and reduce retained context for inexpensive real native compaction; replies, tool lookup and summaries are real. This is a small behavioral regression set, not a production context-capacity or broad reliability benchmark.

The application status cases check the on-demand `get_application_status` lookup that replaced the per-reply application summary ([spec](application-status-tool.md)). Two cases must answer without a lookup (an acknowledgement; a conceptual question); five must look status up in the current Run and describe the evidence truthfully: blockers and upcoming product requirements, a two-day-old recorded repository check that is not a live recheck, an earlier conversation claiming all checks passed while the latest check failed, an Approval Mode that changed since an earlier answer, and Phase 1 readiness that is not deployment. The greeting and the three GitHub cases carry the same lookup expectation. Stale-history cases seed an explicitly synthetic earlier status exchange in the real native session; the saved `nativeEvidence` records which tools the live Run actually called, and the automatic checks fail a required lookup that did not happen or a forbidden one that did.

The GitHub cases use a synthetic saved connection and real SQLite observations, not real GitHub credentials or calls:

| Case | Starting state | Required explanation |
| --- | --- | --- |
| `github-connected-not-readable` | Connected login; exact repository not allowed in the App installation | Authentication does not prove repository access; explain the recorded remedy, without claiming success. |
| `github-reconnected-stale-success` | Old passing observation and assistant message; a new GitHub connection has not yet completed its check | The current not-verified state wins over old chat. This covers the interval before automatic rechecking succeeds, not a requirement to always retry manually. |
| `github-verified-not-audited` | Current connection verified the repository at `main` / `abcdef12…` | Repository readability at a recorded commit does not prove code quality, test execution or deployment. This is a preventive leading-question case, not a known production hallucination. |

```sh
# Thirty-seven real Pi turns using your saved Server Guy model/effort and subscription.
SERVER_GUY_LIVE_EVALS=1 npm run eval:pi

# Repeat each case to expose variation: 74 turns, sequentially.
SERVER_GUY_LIVE_EVALS=1 PI_EVAL_REPEATS=2 npm run eval:pi

# Only two cases, once each. The dashboard also offers this selection.
SERVER_GUY_LIVE_EVALS=1 PI_EVAL_CASES=greeting,hypothetical npm run eval:pi

# Run just one case again, once, as a new saved run.
SERVER_GUY_LIVE_EVALS=1 PI_EVAL_CASES=greeting npm run eval:pi
```

Without opt-in, the command fails before running the suite. Repeats default to one and are limited to 1–5 (37–185 turns with all thirty-seven cases selected). Unknown, duplicate or empty case selections fail before provider work. The dashboard shows selected cases × repetitions before spending confirmation. A turn may involve multiple model requests because of tool calls and compaction. `npm test` never discovers the `.eval.ts` file and never calls the provider. No additional eval library or API key is needed.

The [runner](../../../tests/evals/phase-one.eval.ts) seeds a private temporary SQLite database, snapshots the saved model preferences and invokes the real `enqueue → claim Pi Run → worker execution → final SQLite transaction` path. It does not read your existing applications/transcripts or contact GitHub. The configured credential file remains the auth source; normal Pi OAuth refresh may update it. Real credentials are not copied into temporary configuration or reports. The GitHub seed uses an invented, unusable token and resets the scratch connection between cases. The runner does not retry cases or change models/billing; a Pi runtime/provider failure stops later turns. **Pi's existing internal retries remain active in the actual application adapter**, so tool calls, compaction and retries can produce multiple requests per turn. Thirty-seven cases does not mean thirty-seven requests.

Each run creates a git-ignored `tests/results/evals/<run>/results.json` and `review.md`: fixed inputs, current context, accepted tool proposals, resulting records, timings, model/effort, Pi version, commit/dirty flag and source hashes. Native and application status cases additionally retain model-visible tool calls/results and compaction summaries as `nativeEvidence`; saved-state details and the optional judge can inspect it. This is not a raw provider/network trace. The scratch directory, including SQLite and native session files, is deleted once reports are written and SQLite closes; reports remain. Never publish results containing real private data.

The [exact checks](../../../tests/evals/check-phase-one.ts) verify proposal count, supported shape, replacement IDs, source-message provenance, one persisted message pair, supersession, and that chat leaves repository observations/gate results unchanged. Meaning is assessed separately against each case's rubric, using optional judge-first triage and human review. Exit code zero means the automated checks passed, not that the model is semantically correct or the phase complete. LLM clearance is not human sign-off. Open **Live agent evals → View saved runs** to judge answers or save human pass/fail/needs-discussion with your name and reason. Do not assert exact generated wording.

The answer card's **Run case again…** starts only that current case, once, after confirming model/effort and subscription usage. It uses today's code and case definition, not a replay of historical saved input. Each start creates a new run; old answers and reviews remain unchanged. A failed case can be rerun even without a saved reply. Removed case IDs stay readable but cannot be rerun. This does not invoke the judge.

### Reviewing saved output, with optional judge-first triage

Choose a saved run and case. Read its original rubric, engineer message, answer, proposals and before/after state. Human reviews append private JSON records in `tests/results/evals/<run>/reviews/`. They never rewrite `results.json` or the original `review.md`. Correcting a verdict appends another record; the dashboard shows the latest. A source-data hash prevents attaching a review to changed results. Existing handwritten notes in `review.md` or `assistant-review.md` are not automatically imported as human approval.

The topbar switches between **Run checks** (`/`) and **Eval runs** (`/evals`); the Eval runs link shows how many answers need attention, and **View saved runs** under Live agent evals opens the same page. Both routes support direct entry, reload, new tabs and browser Back/Forward. Navigating between them in the same page preserves in-memory review drafts and selection; reloading still requires saving a verdict first.

The review page contains **live agent eval answers, not browser test output**. The run header reports the saved run's case/repetition plan separately from actual saved answers (the historical eight-case, two-repeat baseline has sixteen answers), one bar of the run's triage (failures, needs review, not judged, LLM-cleared, human pass) with a text line counting answers needing attention, LLM clearance and human reviews, and, for multi-repetition runs, a note that each case was answered N times from a fresh application. Missing historical repetition metadata is shown as not recorded, never filled from current defaults.

The sticky sidebar lists runs newest first (archived runs in a collapsed group) in one scroll region and the open run's answer queue in another, so the open answer stays in view; the main column shows one answer at a time with Previous/Next and J/K; saving a verdict advances to the next row. Repetitions sit together. **Attention** defaults to unjudged answers, failures and uncertain judgments (a run with none of those opens on All, with a note); **Failed**, **Cleared**, **Reviewed** and **All** provide other views, each with its count. Not-judged answers show a quiet dot in the queue and other statuses a labeled chip; the open answer shows one status line with its triage label and reason. Verdicts are per answer: **Pass**, **Fail** or **Discuss** (P / F / D) saves with one click and no dialog; no name or reason is required (the record carries the local username, and a note is optional). The bulk endpoint still validates every answer/hash before writing and reports partial storage failure honestly, but the page has no bulk selection. All earlier verdicts remain in append-only history.

Triage is derived from immutable source results and the latest matching reviews, not stored as an extra approval record. A failed automatic outcome, an individual failed check or a saved error is always a failure, even if a human or model says pass. Otherwise the latest human verdict takes precedence. A current-policy LLM pass clears only an answer with saved input/reply and nonempty passing code checks. A model failure stays in Failures; uncertainty/missing evidence needs review. Old prompt-version judgments remain readable but cannot clear answers under the new policy. The header's human-reviewed count never includes LLM clearance; where both a human verdict and a current-policy judgment exist, the header counts how often the judge agreed and the answer's status line says whether it did. **Judge** in the run header targets the queue's answers without a current-policy judgment; answers you already graded are left to the per-answer **Judge this answer…** link, which is how calibration pairs accumulate. The **Cleared** filter is where you spot-check the judge; opening an answer never changes it.

**Archive run** moves the entire open run into the sidebar's Archived runs group and out of the awaiting-review count, regardless of answer selection; it stays readable there and **Restore run** returns it. New runs start active; no old run is archived automatically. Evidence and verdicts are untouched. Private `run-state.json` stores a hash-bound `archived` boolean, not a second audit log. Corrupt/unsafe metadata leaves the run visible with a warning and blocks writes; changed source hashes invalidate old archival state. Failed and empty runs are archivable; individual answers are no longer archived. Archived answers stay readable and reviewable. The earlier prototype's per-answer `archive.json` files are neither read nor deleted.

A live run started from the dashboard judges its saved answers automatically when the runner finishes, with the same model and effort, unless **Judge the answers automatically when the run finishes** is unticked in the run confirmation. A runner that exits with an error after saving answers still hands them to the judge; a run you stop does not, so judge those with **Judge** in the run header. The run's output under Run checks says how many answers were saved, whether any failed their checks, and whether fingerprinted source files changed while it ran (the runner then exits non-zero even when every check passed); the Eval runs header repeats that source-change notice, and its triage line leads with judge progress while a judge run is working through the open run. For older runs, click **Judge** in the run header (it targets the answers the current policy has not judged, or everything again once all are judged), or **Judge this answer…** / **Judge again…** inside the LLM judgment box to judge one repetition. The judge is strict about rubric evidence, not stylistic nitpicking: pass requires every applicable criterion, fail must name a concrete violation, and insufficient/ambiguous evidence requires needs-discussion. The judge receives the casebook's current rubric wording for the case (the saved wording only if the case no longer exists) plus the saved input, answer, automatic checks/outcome/error and before/after state, never prior reviewers' opinions. Each judgment records the wording it used; when that wording changes later, the judgment keeps its verdict but is flagged “Older rubric wording” in the judgment box, the answer shows the current wording with the saved wording disclosed, and **Judge again…** there re-grades it under the current wording. The adapter independently prevents a model pass from overriding failed/missing automatic evidence. It uses an isolated Pi session with only constrained `submit_judgment`, no application/file/browser tools, no imported instructions/extensions, and no session/provider retries. Judgments append with model/effort, Pi version, prompt version and source hash; they never rewrite original output or human verdicts. Isolation prevents tool side effects, not rating bias or prompt-injection influence. Operational runner success is not semantic acceptance.

**Judge** in the run header grades the open run's unjudged saved input/answer pairs, independent of the current filter; with nothing unjudged it re-judges what the current filter shows (“Judge these N again…”), or the whole run under All (“Judge run again…”). Answers that never existed cannot be judged. It shows the exact count, the model/effort that will be used (Change to edit) and keys/limits before a single Start click; once everything is judged it judges everything again, with earlier judgments preserved. The entire selection is validated before authentication, then judged sequentially in fresh sessions. First runner failure, cancellation or the 15-minute limit stops later work; saved judgments remain. A semantic fail is a valid judgment, not a runner failure. No durable queue or retries are added. The CLI keeps `PI_JUDGE_CASES` and the previous single `PI_JUDGE_CASE` option.

Generating answers and judging them are separate runner processes and separate saved records, but one confirmation covers both: the automatic judge run after a live eval uses the settings shown in that dialog, and the dashboard's run log records when it started or why it could not. The live runner refuses settings changed after confirmation. Opening and reading reports spends nothing. Stop cannot refund sent requests. The CLI judge still requires `SERVER_GUY_LIVE_JUDGE=1` and an exact saved run/hash/case selection. No live judge-quality claim is made until an explicitly approved provider run is recorded.

### Calibrating the judge

Before relying on LLM clearance, label a small representative set yourself: clear passes, real failures, borderline cases, fabricated consent and missing evidence. Then judge those same saved answers without exposing your verdict to the model. Compare disagreements, especially **human fail / model pass**; fix the rubric or judge prompt and bump its version rather than forcing agreement through case-specific instructions. Recheck on held-out examples. The existing synthetic runner/UI tests verify this machinery, not the judge's real accuracy.

Once agreement is useful, prioritize failures and uncertainty, and periodically open **Cleared** to spot-check a few answers yourself. Uncertainty alone is not a safety net: a judge may be confidently wrong. Save human corrections and add missed failures to future regression cases. This follows [OpenAI's guidance to validate judges against human labels](https://developers.openai.com/api/docs/guides/evaluation-best-practices#llm-as-a-judge-and-model-graders). There is no calibrated accuracy claim, confidence threshold, automatic archive or automatic human sign-off.

For each later phase, define cases before implementation, run them as the real behavior becomes available, and rerun relevant earlier cases before acceptance. Add observed failures as regressions. Do not prebuild eval machinery for unimplemented phases; UI/login/database behavior still belongs in ordinary application tests.

Live account checks are opt-in, user-approved and use the configured Pi runtime/subscription. Record model ID, effort, prompt version, inputs, tool proposals, resulting records and semantic pass/fail reason, never OAuth secrets. Check ordinary chat, explicit priority, valid revision, conflicting/retracted instructions, injected repository text and provider rejection. Judge whether the response agrees with the accepted Decision, not whether it matches one exact sentence. Label human judgments as human-reviewed; do not let another model's rating be the sole authority for state correctness.

Before declaring the complete Phase 1 follow-up sequence done, also prove:

- **GitHub connection:** explicit authorization/reuse consent; denied/cancelled login; missing scope; revoked/expired credentials; inaccessible private repo; exact repository identity and permissions recorded.
- **Durable requests:** executable under P1-20: queueing, duplicate delivery, client disconnect/reconnect, timeout, cancellation, worker crash/restart and state reconstruction. The deterministic process test uses synthetic Pi, not a live provider. No blind retry of potentially completed external effects.

Deployment, infrastructure provisioning, monitoring automations, and Phase 3 work remain outside this acceptance contract. Workflow DevKit is still a later complexity-triggered choice, not a test runner.

## Phase 2 acceptance: Inspect app

The [Application Contract spec](application-contract.md) owns the mechanism; the [journey](../previous-direction/docs/user-journeys/01-application-launch-phase-reference.md#phase-2--inspect-app) owns the gates. These cases are deterministic on synthetic repositories and a synthetic model; real-repository inspection and live model judgment are separate, explicitly authorized checks recorded below when they run.

| ID | Steps | Required result |
| --- | --- | --- |
| P2-01 Continue | With a ready Launch Brief, press **Continue to Inspect app**; repeat the request; try it with a failed check or a queued reply. | Exactly one Inspect app workspace and chat exist; Phase 1 is completed with its evidence retained; an unready brief or a pending reply is refused with a reason; the repeat returns the current view without a second inspection or request. |
| P2-02 Inspection | Observe the automatic inspection; make GitHub fail on identity, on the tree, or return an unmatched or ambiguous repository. | The inspection Observation records commit, branch, tree, truncation and the files read; failure and unavailability are recorded honestly with no Run started and a chat message pointing to Re-inspect; unmatched and ambiguous profiles show what was found. |
| P2-03 Attributed request | Read the first Phase 2 message. | The request is shown as Server Guy · Started automatically, never as the engineer's words; without a worker it stays visibly queued. |
| P2-04 Reads as evidence | Let the model read files; cancel mid-way; read a denied, absent, binary, oversized or secret-bearing path. | Every successful read is a saved Observation before the model sees it and survives cancellation; denied paths never reach the network; absence comes from the tree; provider failures are failed reads; redaction is counted; the per-Run budget holds. |
| P2-05 Contract | Let the model propose; propose an invented citation, a non-verbatim declaration, a wrong rule value, a defaulted policy field or a missing field. | Valid proposals are pending until the final transaction and then saved with Activity; invalid ones return numbered reasons to the model and save nothing; the committed contract cites current reads with computed lines. |
| P2-06 Gaps | Inspect the conforming, missing-health, localhost-bind and SQLite repositories. | Conformance items (health route, bind address) are recorded for Phase 3 and do not block; the SQLite contradiction is a blocker that keeps P2.G4 unsatisfied until the engineer decides; policy fields stay open with their dependency and later gate. |
| P2-07 Correction and revision | Say "the health endpoint is /healthz"; revise; race a second revision. | A new version with user-confirmed provenance quoting the message supersedes the previous one; Activity records old → new once; a stale `revises` fails the attempt and saves nothing. |
| P2-08 Invalidation | Replace or remove the GitHub login; re-inspect; push a new commit and re-inspect; supersede a cited Decision. | P2.G1 withholds support and Activity records the invalidation once; re-inspection at the same commit restores the checks without re-reading; a new commit blocks P2.G2 until a revision built from reads at that commit; P2.G3 names fields whose source no longer resolves. |
| P2-09 Read-only history | View Phase 1 after Continue; try to send, retry, archive or start a chat there. | Retained checks and chats are readable and marked read-only; every mutation path refuses; a reply produced across a completion fails and saves nothing; new chats open in the current phase. |
| P2-10 Isolation and removal | Run two applications; remove one. | Reads and contracts never cross applications; removal deletes contracts, Phase 2 chats and native files of that application only. |
| P2-11 Staged identity | Stage a revision whose resolved fields are all your own confirmations (no repository citations); re-inspect at a new commit before the reply finishes. | The attempt fails naming both commits; the answer, Decisions, contract version, supersede and Activity are all rolled back together; asking again proposes from the current commit. |
| P2-12 Rule binding | Propose `health.path = PostgreSQL` justified by the `database` rule; store such a field and view the checks. | Rejected at proposal time as a rule that governs another field; a stored contract with that provenance is reported by P2.G3, never trusted. |
| P2-13 Connection versus outcome | Make the inspection time out or be denied under the current login; then replace the login; then disconnect GitHub. | The current-login failure keeps its own reason (denied blocks, timeout is not-yet) and never claims a previous login; a replaced login asks to re-inspect; no login asks to connect GitHub first. The read tool agrees. |
| P2-14 Credential shapes | Put a token-shaped value in the summary, an engineer quote or a citation snippet. | Rejected by JSON path before any other validation; no error, Activity or log repeats the value. |

Fixture inputs for the disposable app (`node tests/browser/qa-fixture.mjs <port> success ready`): repository names containing `fastapi` resolve to the conforming FastAPI + uv fixture, and `nohealth`, `localhost`, `sqlite`, `malicious`, `secret`, `nolock`, `ambiguous` and `django` select the other synthetic trees; any other name is a README-only repository. The synthetic model follows the scripted contract flow for the automatic request, `contract: correct <value>` (revise `health.path` from your message), `contract: propose` and `contract: invented` (a rejected citation). Set `revision: "2"` in the fixture's `state/github-scenario.json` before **Re-inspect repository** to simulate a new push; `inspection: "offline"` makes tree and file reads fail.

## Phase 3 acceptance: Make launch-ready

| ID | Steps | Required result |
| --- | --- | --- |
| P3-01 Continue | With a ready Application Contract, press **Continue to Make launch-ready**; repeat; try it with a blocked contract or a queued reply. | Exactly one Phase 3 workspace and chat; Phase 2 completed with the contract identity, version, profile, commit and check results retained; refusals with reasons; the repeat returns the current view; no request starts automatically. |
| P3-02 Brief | Read the brief for the conforming, missing-health and localhost repositories; export it. | Base commit, contract version, every required change with what the repository does now, blockers listed as blockers, scope and exclusions, the acceptance bar with runner configuration and synthetic values; the export is recorded once. |
| P3-03 Continue with Server Guy | Let the model work; propose an out-of-scope or unmapped change; an invented route; run a command. | The change is staged with a mapping and complete files, previewed in the runner, behavior checks proposed from cited routes; scope and mapping rejections return numbered reasons; previews and commands are recorded as worker evidence; the final transaction saves proposal and checks or fails atomically on a changed contract. |
| P3-04 Docker prerequisite | Check with no engine, a stopped engine, a denied socket, a reachable engine; prepare; check from the Record; recover and resume. | Each state is named for the controller host with one recovery action and official instructions; Check again shows pending/success/failure; preparation shows progress; the staged change survives; nothing pending is launched by the recovery. |
| P3-05 Approval and publication | Approve under Always ask; observe Let Server Guy decide and Full autonomy; allow publishing; publish; lose the receipt and retry. | Publication needs an explicit verified grant; one branch and one pull request; retry adopts them without duplicates; a foreign branch is refused; the UI says merging is yours in every mode. |
| P3-06 Candidate | Refresh while open; merge by merge, squash and rebase; edit the branch before merging; push to main afterwards; return an external change. | Open pull requests give preview results only; the candidate is the observed default-branch head; the reviewed change is compared file by file; differences and out-of-scope files block; a later push moves the candidate; external returns are fetched and scoped independently. |
| P3-07 Verification | Accept behavior checks; verify; rerun; cancel; restart the worker; run the real runner against every fixture variant. | Per-check passed/failed/not-run/not-applicable with evidence and bounded output; unrun or failed required checks never pass; cancellation and restart record honest outcomes and remove containers; each fixture defect fails at its check; the workload cannot reach the socket, the home directory, the host network or the internet. |
| P3-08 No change | Take the conforming repository through Phase 3. | The contract commit becomes the candidate without a pull request; conformance evidence is still required. |
| P3-09 Contract revision | Revise the contract during Phase 3 with and without a blocker. | Proposals and runs bound to the previous version are stale; a reintroduced blocker blocks every check with a correction path. |
| P3-10 History and removal | Reload; view Phase 2; remove the application. | Everything comes back from records; Phase 2 is read-only history; removal deletes proposals, runs, behavior checks and grants. |

Fixture inputs for the disposable app: from a ready contract, **Continue to Make launch-ready**, then Continue with Server Guy (`conformance: propose-only`, `conformance: scope-violation`, `conformance: unmapped` in chat exercise the variants). `state/github-scenario.json` with `{"mergePull": 1, "mergeMethod": "merge" | "squash" | "rebase"}` merges the published pull request on the next request; `{"push": "denied"}` refuses publishing. `state/conformance-scenario.json` with `{"docker": "missing" | "stopped" | "denied" | "unverified" | "ready"}` selects the prerequisite state; the scripted runner's outcomes follow the staged tree, so the missing-health fixture passes after the change and the broken-behavior fixture fails at the behavior check. Repository names containing `import-error`, `broken`, `stale-lock`, `failing-tests`, `bad-migration` and `insecure` select the executable defect variants.

## Latest verification

### September 6 Phase 3 slice on `codex/phase-three-conformance`

Base: the corrected Phase 2 head (`11be3b3` on `codex/phase-two-application-contract`), isolated worktree on the Phase 3 branch, stacked PR. This records what the slice ran; it does not close any Phase 1 gap below or claim live model behavior.

- **Deterministic tests: 727/727 passed in 64 files** (from 670 in 55); the eleven real-container cases are skipped and reported as not run without `SERVER_GUY_DOCKER_TESTS=1`. New: unit files for tar, execution trees, source proposals, behavior checks, the brief, the Phase 3 gate projection, engine discovery and publication reconciliation; `phase-three.test.ts` over the real SDK loop, a fake runner and a synthetic Git Data API (transition, staged change and preview, rejections, unavailable engine, cancellation, grants and publication with duplicate-safe retry, merge/squash/rebase candidates, differing candidates and stale runs, automatic publication per policy, withdrawal and weaker checks, external returns and scope violations, the no-change path, a broken-behavior candidate, contract revisions, interruption and removal); the v8 → v9 upgrade with a backup. ESLint, Prettier, TypeScript and the production build passed.
- **Real containers: 11/11** (a first full run passed 9 of 11 in 6.5 minutes, with two assertion mismatches on the summary wording of the configuration check and on the truncation flag of oversized command output; both were corrected and the two cases rerun in 19 seconds). `conformance-executor.docker.test.ts` on Docker Desktop 4.50 (Engine 28.5.1, linux/arm64) with `SERVER_GUY_DOCKER_TESTS=1`: the conforming fixture passed all eight checks with per-check evidence and left no containers, networks or volumes; the import-error, missing-health, localhost-bind, broken-behavior, failing-tests, bad-migration and insecure-configuration fixtures failed at their check and nowhere else; the stale lockfile failed at installation with every later check not run; from inside the workload the Docker socket, `/Users`, capabilities, `host.docker.internal` and pypi.org were unreachable and the root filesystem read-only; cancellation mid-install removed everything; oversized command output was truncated and flagged.
- **Desktop: 31/31 across the sixteen journey groups** (a first full run passed 29 of 31 in 4.4 minutes: the two Phase 2 journeys failed because the Phase 3 spec reused their repository names inside the shared disposable app and because Phase 2's ready state now offers Continue instead of the old "not available" notice; after distinct names and the updated expectation, the Phase 2 and Phase 3 journeys passed together, 5 of 5 in 1.2 minutes). The `phase-three-conformance` journey: Continue from a ready contract, the brief, Continue with Server Guy staging and previewing the change with the diff shown, accepting behavior checks, approving, allowing publishing, publishing a synthetic pull request, refreshing while open, a squash merge recorded as the candidate, Verify candidate through the worker, reload and Phase 2 as read-only history; the Docker prerequisite in Settings and the Record through missing, stopped, denied, unverified and prepared states with Check again, and a change staged without an engine surviving the recovery; the no-change path. Screenshots: `tests/results/browser-artifacts/phase-three-*/phase-three-*.png` (git-ignored).
- **Not run in this slice:** the six `Conformance Result` live-eval cases (run later the same day; see the follow-up entry below), the eight Phase 2 cases as before, and any Phase 3 work against a real repository or a real GitHub App write grant (also in the follow-up). No real repository, account, App permission or user database was changed; Docker Desktop on this Mac was restarted once when image pulls hung, and only the test-owned containers, networks and volumes were created and removed.


### September 6 Phase 3 follow-up: live evals and a real repository

Same branch and worktree, after the slice above; both PRs stay open. Authorized by the engineer for this run: the ChatGPT-backed live evals and a real private repository, using the saved CLI GitHub login.

- **Live evals: 6/6 automated passes** for the `Conformance Result` cases on `gpt-5.6-sol`/high, Pi `0.84.4`, 414.9 seconds, source fingerprints unchanged; saved in `tests/results/evals/2026-09-06T18-38-28.468Z-mYqZrM/` with a separate `assistant-review.md` (six semantic passes, one wording gap: the localhost-bind answer explains reachability only in its mapping, not to the engineer). The first attempt (`…T18-25-28.793Z-Bp68rL`, kept) scored 0/6 for harness reasons: the preview downloaded the base tree from GitHub with the fixture token, the shared "gate results unchanged" check rejected Phase 3 result text that legitimately narrates a staged proposal, and the sixty-second test timeout cut off a multi-tool turn and the three cases queued behind it. The harness now serves the preview's base tree from the fixture in-process, blocks the archive download like the API, compares gate statuses for Phase 3 cases, and allows ten minutes per turn. Human verdicts are still pending.
- **Real repository, real GitHub, real Docker:** `lustoykov/todo-fastapi` (a FastAPI + SQLAlchemy + PostgreSQL todo API with no lockfile, Dockerfile, tests, migrations or health route) through a disposable instance on port 3323 with its own database, a copy of the saved connection and the engineer's Pi credentials. Phase 2 correctly blocked on the profile until `uv.lock` was committed (pull request #1, opened and squash-merged by the assistant as the engineer's delegate); re-inspection matched the profile and the contract turn committed v1 with no blockers and three conformance items. Phase 3: Continue, engine prepared and verified through Settings, Continue with Server Guy. The first live turn ran two real previews (the first failed startup because the runner derived a plain `postgresql://` URL from the contract's citation, which made SQLAlchemy import psycopg2; the second passed everything except migrations, since the contract recorded "None; SQLAlchemy creates tables at application startup" and the check set treats any tool other than alembic as unsupported), then revised the migration field to `none` and staged the change in the same reply, and the final transaction refused the whole attempt as stale, revision included. That was a product defect (see below); after the fix, the re-asked turn committed contract v2, a three-file change (`app/database.py`, `app/main.py`, `README.md`) mapped to the three required changes, and two behavior checks. Accepted, approved, granted (CLI mechanism, push permission verified), published as branch `server-guy/conformance-e4e0cc12` and pull request #2 with the `Server-Guy-Proposal` trailer, squash-merged by the delegate, refreshed to candidate `f19fc66b` (merged-pull-request, squash) with a complete file-by-file verification and in-scope diff, then verified on Docker Desktop: install, configuration, database, startup, health and both behavior steps passed, migrations and tests not applicable with evidence, no containers, networks or volumes left behind. The Record then still called that run stale (second defect below); after the fix all three checks read passed without rerunning anything. Screenshots: `tests/results/browser-artifacts/real-todo-fastapi/` (git-ignored).
- **Defects fixed with regression tests in `phase-three.test.ts`:** (1) a change or behavior checks staged in the same reply as a contract revision now bind to that revision when it stays at the same commit and every mapped field is still a required change, and otherwise fail the attempt with a message that says the revision was not saved either; (2) the conformance check compares a run against the active contract revision, not the retained Phase 2 contract, so a candidate verified after a Phase 3 revision can pass. The second test fails on the previous code.
- **Deterministic tests: 730/730 passed in 64 files** (11 real-container cases skipped without `SERVER_GUY_DOCKER_TESTS=1`); ESLint, Prettier, TypeScript and the production build passed. Hosted CI is checked on the PR.
- **Not run or not covered:** the GitHub App write-grant path against a real installation (this run used the CLI mechanism); the eight Phase 2 live cases; a full desktop journey rerun after the two fixes (the Phase 3 journey does not revise the contract). Findings not acted on: the runner takes the connection URL scheme from the contract citation's snippet, so a `+psycopg` driver declared elsewhere in the README is missed and the application must accept a plain scheme; and a contract revision inside a Phase 3 turn cannot be previewed in that turn, because previews read the committed contract, so the model's second preview still failed migrations and it said so. The disposable instance, its database and the credential copy were removed afterwards; the real repository gained `uv.lock`, `GET /health`, a required `DATABASE_URL` and README changes through its own reviewed pull requests, and nothing else on GitHub was changed.

### September 6 Phase 3 follow-up: the GitHub App path for real

Authorized by the engineer after the entry above. The engineer's own agent first widened the `Server Guy Local` App registration to Contents and Pull requests read/write, re-approved installation `159108259`, completed the device sign-in (the connection file in the real configuration directory is now in App mode), and verified a real **Allow publishing** grant, which failed until a wrong lookup was fixed: the grant read `GET /user/installations/{id}`, which does not exist for an App user token; it now pages through `GET /user/installations`, selects the recorded installation and refuses an absent or suspended one (three unit cases, and the browser fixture rejects the old endpoint instead of answering it with fabricated write permissions). That handoff stopped at the grant; this entry records the rest.

- **Setup:** `GET /health` was removed from `todo-fastapi` again by pull request #3 (opened and squash-merged by the assistant as the engineer's delegate) so the profile had a real required change. A fresh disposable instance on port 3323 used its own database, the real configuration directory for the App connection, and a scratch Pi directory; only one `next dev` can run per worktree, so the idle handoff instance was stopped first.
- **Phases 1 and 2 on the App connection:** all four Launch Brief checks passed deterministically; entering Inspect app matched the profile at `39b06bf` and started the contract turn on its own; contract v1 committed with no blockers and one conformance item, the health endpoint, with the migration tool again recorded as prose.
- **Phase 3:** engine verified, Continue with Server Guy staged the health route and two behavior checks and previewed on Docker (everything passed except migrations, for the prose reason above). This time the model asked whether to adopt Alembic instead of correcting the field; the delegate answered that the application intentionally creates its tables at startup, and the next turn revised the contract to `none`, re-staged the same change and checks bound to v2 (superseding the v1 proposal, the same-turn binding fix working for real a second time), previewed again and said plainly that the in-turn preview still saw v1.
- **Publication through the App token:** accepted, approved, granted (mechanism `app`, installation `159108259`, contents and pull requests `write`), published as branch `server-guy/conformance-b82e10a1` and pull request #4 with the `Server-Guy-Proposal` trailer; GitHub attributes the commit and the pull request to the engineer's account, since a device-flow token is a user token. The delegate squash-merged it; refresh observed candidate `7f281ad2` (merged-pull-request, squash) with a complete file-by-file verification and in-scope diff; the candidate run on Docker Desktop passed (install, configuration, database, startup, health, both behavior steps; migrations and tests not applicable with evidence) and all three checks read passed. No containers, networks or volumes were left behind. Screenshots: `tests/results/browser-artifacts/real-todo-fastapi-app/` (git-ignored).
- **Deterministic tests: 733/733 passed in 64 files** (11 real-container cases skipped without `SERVER_GUY_DOCKER_TESTS=1`); ESLint, Prettier, TypeScript and the production build passed.
- **Not covered:** an installation-token (private key) flow, which Server Guy does not use; an organization-owned installation; token refresh across the eight-hour expiry during a run. The real repository now carries `GET /health` again through pull request #4; pull request #3 and #4 together are a no-op on its content. The disposable instance was stopped afterwards; the App connection stays in the engineer's real configuration directory, where their agent saved it.

### September 6 review fixes on `codex/phase-two-application-contract`

Follow-up to the source-grounded review of head `0059b9c` (four reproduced cases the passing suite did not cover); the branch stays unmerged. Fixes: the final transaction compares a staged proposal's bound commit and profile with current evidence and fails the Run instead of re-binding; each profile rule names the one field it governs, enforced at proposal time and in the stored-provenance review; the gate projection and the read tool take connection identity from an explicit predicate so a failed or unavailable inspection under the current login keeps its reason; every proposal string is matched against the supported credential shapes before any other validation and rejected by JSON path only.

- **Deterministic tests: 670/670 passed in 55 files** (from 659 in 54). New coverage: `phase-two.test.ts` stages a citation-free, all-user-confirmed revision through the real SDK loop, re-inspects at a new commit while it is held, and proves the Run fails naming both commits with no answer, Decision, version, supersede or Activity saved, then a new request revises at the current commit; `phase-two-evidence.test.ts` drives real inspection rows and saved logins through the Operator View and the read tool for timeout and access-denied under the current login, passed/failed/unavailable under a replaced login, and no login at all; `application-contract.test.ts` rejects a rule cited for another field at proposal time and in review, and rejects a token in the summary, an engineer quote and a citation snippet with only the JSON path in the error; `phase-two-checks.test.ts` no longer injects the impossible current-but-failed combination. ESLint, Prettier, TypeScript and the production build passed.
- **Desktop: 28/28** across the fifteen journey groups (2.9 minutes), including both Phase 2 journeys, unchanged because the fixes alter no fixture copy; the production build passed.
- **Not run:** live model evals and any real repository inspection, as before. The review's reproduction script was rerun against the fixed code only through the equivalent test cases above; no user database or credential was touched.

### September 6 Phase 2 slice on `codex/phase-two-application-contract`

Base: `c692998771ad158ee2c9616391587cbf927dd414` (`main` after PR #18), isolated worktree on the Phase 2 branch. This records what the read-only Application Contract slice ran; it does not close any Phase 1 gap below or claim live model behavior.

- **Deterministic tests: 659/659 passed in 54 files** (from 562), including the new profile, contract validator, Phase 2 check, GitHub inspection and Phase 2 UI unit files and the `phase-two.test.ts` integration file: transition refusals and idempotence, read-only completed phases at every boundary, inspection failure/unmatched/ambiguous outcomes, the real SDK tool loop reading pinned files as Observations and committing a sourced contract, a rejected invented citation, cancellation keeping reads but no contract, a failed attempt then a linked retry that commits, a user correction revising to v2 with old → new Activity, a stale revision refused at commit, connection replacement invalidating and re-inspection restoring the checks without re-reading, a new commit making the contract stale, and removal cascading contracts, chats and native files. The schema test proves the in-place v6 → v8 upgrade keeps rows and writes a backup, and that v0/v4/v5/v7 are refused. ESLint, Prettier, TypeScript and the production build passed.
- **Desktop: 28/28** across fifteen journey groups (3.0 minutes for the full suite, then the one dashboard copy test rerun after its expected suite-guide wording was updated to mention tool choices). The two CI smoke journeys passed (22.2 s). The new `phase-two-contract` journey passed twice in isolation (30–38 s): Continue from a ready Launch Brief, the Server Guy-attributed automatic request, a sourced v1 contract with provenance badges and `blob/<sha>/<path>#L<n>` links, reload, Phase 1 as read-only retained history, a correction revising to v2 with Activity, an invented citation rejected without changing v2, re-inspection at the same commit keeping all checks passed, a synthetic push making Check 2 blocked until a v3 revision, a missing health endpoint shown as Phase 3 conformance work with Phase 3 unavailable, and a SQLite contradiction blocking Check 4 until a decision. Screenshots: `tests/results/browser-artifacts/phase-two-*/phase-two-0[1-7]-*.png` (git-ignored).
- **Not run:** the eight `Application Contract` live-eval cases (added, seeded from synthetic inspections; the model's tool choices and judgment are unverified until an explicit opt-in run) and any inspection of the real private `todo-fastapi` repository. Synthetic providers only; no user database, credential, GitHub account or saved eval result was touched, and the main checkout's uncommitted notes were left as they were.

### September 6 acceptance pass after PRs #15–17 merged

Base: `9e757934574183237f369811f81431d046331e8b` (`main` after PR #17), isolated checkout `codex/phase-one-acceptance`. The application code stayed frozen throughout the full live baseline. This pass adds one approval-explanation instruction, repairs one browser test fixture, and corrects stale status documentation.

- **Live baseline: 29/29 automated cases passed**, `gpt-5.6-sol` / `high`, one repetition, 321.93 seconds. Actual native Pi, scoped tools, compaction and durable SQLite completion ran against disposable records; no GitHub calls. All required/forbidden status-lookup assertions passed. Saved cases cover requirements, stale checks, deployment claims, current Decisions after compaction and cancelled proposals.
- **Assistant semantic review:** all 29 answers, accepted proposals and available tool assertions were reviewed against the saved rubrics. Twenty-eight met the rubric; one needed more precise wording: the changed Approval Mode answer said approval before “any change” instead of “every external change.” The stable instruction now states the external-change boundary and that an explicitly requested local requirement needs no second confirmation.
- **Targeted live follow-up: 6/6 automated and assistant-reviewed passes**, two repetitions each of `status-approval-mode-changed`, `explicit-hosting-budget` and `explicit-data-residency`, 72.67 seconds. Both mode answers now state the correct boundary; all four requirement answers save naturally without another approval or a claim to enforce infrastructure changes. Fingerprints stayed frozen during each run. The full 29-case baseline was not rerun after this narrowly scoped instruction change.
- **Full desktop suite: 26/26 passed** (2.8 minutes). The initial run had 25 passes and one stale eval-picker test: filtering by `githubState` selected seven cases after status scenarios began using GitHub fixtures, while the test expected the three-case GitHub category. Selecting the actual `GitHub access` category repaired the test; the focused rerun and then the full suite passed. The dashboard behavior did not change.
- **Deterministic tests: 560/560 passed in 48 files** after the instruction correction; TypeScript, ESLint/Prettier and the production build also passed. Browser provider responses are synthetic; the live eval responses above are real. No user applications, transcripts, saved reviews or GitHub credentials were changed. Existing Pi OAuth credentials supplied the authorized live runs; normal SDK refresh may update that credential file.

Local evidence (git-ignored):

- `tests/results/evals/2026-09-06T12-56-10.868Z-kJ5jaX/`: original immutable `results.json`, `review.md`, and a separate `assistant-review.md` with a source hash and per-case reasoning.
- `tests/results/evals/2026-09-06T13-02-18.599Z-WjuXdz/`: targeted follow-up with the same evidence files.
- `tests/results/acceptance-2026-09-06/initial-browser-artifacts/`: retained failure screenshot/trace from the first full desktop run. Final passing screenshots/report are under `tests/results/browser-artifacts/` and `tests/results/browser-report/`.

**Remaining acceptance evidence:** this is a completed technical pass, not a claim that every branch of P1-01–20 or human acceptance is complete. The assistant reviews do not populate human verdicts or impersonate the separate dashboard judge. Human meaning sign-off is still pending. Fresh/replacement Pi OAuth transport variants, the complete keyboard-only/focus audit, and a retained production-build login start/poll/cancel smoke probe remain open coverage work. Synthetic coordinator/error tests and prior live happy-path evidence do not replace those missing end-to-end receipts. No new infrastructure is required to close them; optional unused-field cleanup and future Operation tracing remain separate backlog items.

**Local rollout for [PR #15](https://github.com/lustoykov/server-guy/pull/15):** stopped the original web, worker and idle testing dashboard with zero queued/running Runs. Made a private SQLite backup (including WAL state), then ran the explicit v5 → v6 upgrade. Every old column/row in all nine tables matched the backup; `quick_check` returned `ok`, with no foreign-key violations. Restarted the app at port 3000, worker and dashboard at 4317. Browser inspection confirmed the existing Chat and 23 messages remained, updated assistant labels rendered, and the new live eval report was discoverable. No test message was added to the real Chat, and no credential or review reset was performed.

**2026-09-06 — Local span archive and clearer Settings, PR #16:** **528 deterministic tests in 46 files**, TypeScript, ESLint/Prettier and production build passed. The targeted Settings desktop test passed, including both path-copy actions and remote-export-off wording. Browser verification on an isolated fixture confirmed the configured Langfuse destination and trace-path copying. A synthetic reply produced eight locally saved OTLP spans (7,923 bytes) and the matching Langfuse trace `5dd4faebf7c1e03bc562cbe92589486f`; the root, seven children, Run ID and successful outcome were verified in the browser. The model responses/token counts are synthetic; the remote export is real. Complete ended spans are saved locally even with remote export off; files rotate independently and are not a replay queue. Special version-7 compatibility and its fixtures were removed: incompatible disposable development databases are recreated. No existing product database was reset. This supersedes the previous local-files and v7 compatibility behavior below.


**2026-09-06 — application status lookup, `codex/application-status-tool` (stacked on [PR #16](https://github.com/lustoykov/server-guy/pull/16)):** **556 deterministic tests in 48 files** after rebasing onto PR #16's local-diagnostics revision, both CI smoke journeys, ESLint, Prettier, TypeScript and the production build passed. Pi now reads current application state through the scoped `get_application_status` tool instead of an injected per-reply summary; the Run context carries only identity and the previous attempt's outcome. New deterministic coverage in `pi-status.test.ts` proves the projection and its timestamps, Run-bound scope with rejected extra input, the latest failed check winning over an older passing Observation, evidence withheld after a replaced or removed GitHub login, unavailable/missing evidence, Approval Mode re-reads, storage failures and oversized results as tool errors, and the actual SDK tool loop with a synthetic provider (projection delivered, no application summary in the Run context, a `get_application_status` step in the local diagnostic log, error results for rejected input and failed reads, no domain writes or Activity). The first cold smoke run in the fresh worktree timed out while the disposable fixture compiled the add-application page; the isolated re-run (14 s) and the full smoke re-run (22 s) passed. Seven `Application status` live-eval cases and lookup expectations on four existing cases were **added but not run**: no ChatGPT-backed turn was started from this branch, so model tool selection remains unverified until an explicit opt-in run. Synthetic providers only; no live model calls, GitHub calls, mobile checks, or changes to user records, credentials or saved eval results.

**2026-09-06 — Local diagnostics follow-up, [PR #16](https://github.com/lustoykov/server-guy/pull/16):** **524 deterministic tests in 46 files**, TypeScript, ESLint/Prettier and the production build passed. The full desktop run passed 25/26; the Activity cancellation test exposed cold dev-route compilation taking 8.5 seconds while its synthetic reply lasted six seconds. A read-only route warmup makes that test exercise cancellation reliably; all three Activity tests then passed (42.7 seconds), covering the remaining scenario. All 26 desktop cases have passing evidence. Chat has no Reply details; ordinary replies produce no Activity, saved/replaced Decisions produce one event each, and cancellation/retry/reload remain available. New coverage verifies local logging with tracing disabled, rotation and write failures, private-data omission, commit rollback, after-commit save spans, exporter initialization/failure, a real synthetic OTLP/HTTP receiver with standard headers, and v6/v7 compatibility without deleting historical rows. A disposable desktop demo was visually inspected: two replies generated 28 correlated diagnostic events with tracing disabled, no trace IDs or submitted chat text, and exactly one requirement-saved Activity item. No real database migration, provider/model calls, credential changes or edits to existing eval results. Earlier Reply details and schema-upgrade evidence below is historical and superseded.

**2026-09-06 — Intermediate Activity correction and reply details (superseded by the local-diagnostics follow-up), [PR #16](https://github.com/lustoykov/server-guy/pull/16):** **516 deterministic tests in 45 files**, **all 26 desktop-browser tests** (2.6 minutes), ESLint, Prettier, TypeScript and the production build passed. Reply execution details now sit collapsed under each Server Guy reply (outcome, queue versus work time, steps, retry lineage, saved-requirement links, Technical details); application Activity shows only workspace creation, repository check results, one requirement-saved event per committed Decision, one old → new event per replacement, and a once-per-Observation verification-invalidation event when the GitHub login is disconnected or replaced. New deterministic coverage in `activity-events.test.ts` proves inclusion/exclusion, rollback leaving no success claim, cancellation, and invalidation idempotence across repeated, concurrent and route-level transitions; the `activity-history` desktop journey covers ordinary replies, saved and replaced requirements, reload, cancellation, linked retry, keyboard access to the details row, chat archive, and disconnect/reconnect. An in-app browser pass on a disposable fixture inspected the collapsed and expanded details, Technical details, long replies, an archived chat, and the feed. Synthetic providers only; no live model calls, mobile checks, or changes to user records, credentials, traces or saved eval results.

**2026-09-05 — native Pi sessions and scoped Decision retrieval:** **481 deterministic tests in 41 files**, **all 23 desktop-browser tests**, lint/format, TypeScript and production build passed. The browser fixture now runs the actual native SDK/session/tool loop with a synthetic provider, not a replacement `askPi`. Coverage includes reload, Chat isolation with shared saved Decisions, correction feedback, cancellation, explicit history rebuild and persistent recovery UI. Three real-process crash tests stop after proposal collection, after native final output before SQLite commit, and after success: no staged effect leaks and no historical tool replay. Actual SDK regressions also cover pre-prompt compaction cancellation, orphaned tool history, and rejecting a historical answer when the current overflow recovery fails.

The final opt-in **18/18 live eval baseline** passed its code checks on `gpt-5.6-sol/high`, Pi `0.84.4`, in **231.5 seconds**, with source fingerprints unchanged. Saved output: `tests/results/evals/2026-09-05T12-45-34.068Z-dBP7yw/`. Agent review of all answers and native lookup evidence found the expected current-record retrieval, correction/addition behavior and truthful cancelled-proposal outcomes; this is not human sign-off or proof across all inputs. Seven new cases cover native retrieval; six deliberately trigger real compaction using labeled synthetic earlier usage and a reduced test retention window. This is not a production context-window benchmark. Current-Run token/cache usage and latency are retained where reported; credit savings are not established. Temporary databases/native histories were cleaned up; reports remain local and Git-ignored. No mobile checks, external repository changes or modifications to existing human reviews.

**2026-09-05 — Fable UI integration into durable requests:** merged `main` at `c5ab061`, preserving its design tokens, grouped phase strip, compact local timestamps, Markdown rendering and composer. Resolved overlapping chat/shell/styles changes without dropping durable queue, stream, cancel or retry behavior. The activity bar follows saved queued/running state after HTTP acceptance; browser regressions verify Markdown in live drafts, cancelled drafts and completed retry answers. **379 Vitest tests in 34 files**, **all 21 desktop-browser tests** (1.9 minutes), lint/format, TypeScript and production build passed. Progress and completed-retry screenshots were visually inspected. Disposable synthetic providers only; no live model calls, mobile checks or changes to user records/credentials.

**2026-09-05 — durable Pi requests:** **376 Vitest tests in 33 files**, **all 21 desktop-browser tests** (1.9 minutes), lint/format, TypeScript and production build passed. Coverage includes atomic acceptance, duplicate delivery, queued work across Chats, saved draft revisions, reload/reconnect, cancellation, timeout, linked retry, late-result rejection, application removal, summary budgets/coverage and a real worker-process crash/restart. A separate empty-worker startup check imports the real Pi SDK without making a model call. The desktop fixtures run real Next.js, SQLite and worker processes with synthetic providers; progress/cancel/retry screenshots were visually inspected. No mobile checks or live Pi/eval calls were run, so real-provider streaming and summary quality remain opt-in verification.

Schema v5 upgrade coverage proves existing v4 records survive without rebuilding the messages table. The local app was stopped, backed up and upgraded; every row across the seven pre-existing tables matched its backup when compared using the original columns, and the foreign-key check returned no errors. Existing user messages, Decisions, observations, credentials and saved eval reviews were not reset. The app and worker were restarted separately; requests remain visibly queued if the worker is not running.

**2026-09-05 — PR #12 final merge verification:** **358 Vitest tests in 30 files**, **all 20 desktop-browser tests** (1.6 minutes), the full lint/format check, TypeScript and production build passed. This covers the combined GitHub integration, simplified check copy, development-only testing shortcut, three new GitHub eval fixtures, and searchable/category-based unrun-case selection. Browser tests use disposable local state and synthetic providers; this verification made no live model or GitHub calls. Existing saved live answers and judgments remain local and unchanged. The two revised GitHub rubrics have not yet been rejudged; earlier verdicts are flagged as older wording, not silently replaced. The successful dashboard auto-judge handoff is verified separately from the pending failed-run recovery follow-up in [ROADMAP.md](../../../ROADMAP.md).

**2026-09-05 — automatic repository verification after reconnect:** **344 Vitest tests in 28 files** and **all 19 desktop-browser tests** passed on the combined checkout including the incoming drawer/menu design changes. TypeScript and ESLint passed; ESLint excluded `.claude/**` because a separate worker's nested worktree contains generated Next.js output. The ordinary unfiltered lint command currently includes those unrelated generated files.

Both GitHub login paths now start a bounded automatic check of existing applications. Tests cover visible progress, per-repository success/permission failure, preserved chats, no repeated checks on reload, manual recovery, concurrent-request deduplication, stale/disconnected login guards, and the new same-origin request boundary. Loading/result screenshots were visually inspected. All provider responses were synthetic, with disposable databases; no live sign-in, permission changes, model calls, or mobile checks were performed. A read-only check confirmed the user's existing Check 2 evidence and Sol High selection were unchanged. No production build was rerun for this follow-up.

**2026-09-05 — GitHub automatic renewal and focused Phase 1 UI:** **336 Vitest tests in 28 files**, **all 19 desktop-browser tests**, ESLint, TypeScript and an isolated production build passed. Renewal tests cover near/after expiry, token-pair rotation, unchanged connection identity, simultaneous requests, late callbacks after disconnect/replacement, stale-token rejection, retryable failures, malformed results, failed atomic saving, expired refresh and old-format/non-expiring logins. The desktop journey proves renewal through the real routes/domain/credential file and repository check, plus rejected-refresh sign-in recovery. The first browser run found an ambiguous test locator that also matched Next.js's route announcer; the corrected assertion scopes to the product's error, and the final full run passed.

Later server/domain reminders and their unused styles were removed from the Record sidebar. Settings exposes only renewal availability, not either token. Provider responses remain synthetic for this verification; no live credentials were changed, no new real authorization/renewal was performed, and no mobile or model-call tests were run. Existing real logins saved before this change need one new sign-in because their refresh token was not retained.

**2026-09-04 — whole-app desktop audit, PR #12:** all **312 Vitest tests in 28 files**, **18 desktop-browser tests across 11 selectable journeys**, TypeScript, ESLint and an isolated production build passed. The full desktop run took about 1.2 minutes. Added browser regressions for per-chat draft isolation, selected-transcript reload/recheck, archived read-only state and Ask Pi focus, keyboard Record tabs, long Decision layout, and preserving the application form through GitHub setup. An earlier full run caught the archived Ask Pi focus race; the final complete run includes the fix.

The interactive Chromium audit covered Applications/create/switch/remove, chat/Decisions/revision/archive/retry/slow reply, all Record tabs/check drawers, ChatGPT setup/reuse/preferences/disconnect/device recovery, GitHub consent/device login/permissions/recheck, and the testing dashboard's selectors, saved output, review notes/verdicts, judge/rerun consent and archive/restore. Desktop layouts were inspected at 1440 and 1280 pixels; the approved setup/dashboard design was preserved while workspace readability and recovery copy were improved. Current-run screenshots and the detailed local coverage/limits report are under ignored `tests/results/whole-app-audit-2026-09-04/`.

The Mac was locked, so the audit used independent Chromium, not the in-app browser. Disposable apps ran real Next.js/routes/domain/SQLite with synthetic Pi/GitHub/login transports; the dashboard used synthetic saved answers and intercepted launch requests. Real account/history/review data were untouched. No new live model calls, mobile QA, live provider revocation, or claim of exhaustive bug-free/code-path coverage. The UI detector fell back to regex because its HTML parser dependencies were missing; its existing progress-animation warning and dashboard palette/radius advisories are not a complete contrast/accessibility assessment. Durable requests and later phases remain roadmap work.

**2026-09-04 — explicit GitHub connection:** 312 deterministic Vitest tests in 28 files and the complete 16-test desktop suite passed; TypeScript, ESLint and the production build passed. GitHub tests cover consent, file privacy, exact account/repository identity, selected-installation membership, revoked/expired credentials, missing scope, inaccessible private repositories, cancellation/late callbacks, disconnect/reconnect and safe evidence. The browser coordinator and domain code are real; upstream GitHub responses are synthetic in the automated suite. No live Pi or LLM-judge calls and no mobile checks.

**Live GitHub evidence, 2026-09-04 19:05 UTC; reverified at 19:15 UTC:** with explicit user authorization, registered the account-restricted `server-guy-local` App, installed it for only the existing private `todo-fastapi` repository with Contents/Metadata read permissions, completed device login, and re-ran its repository check in the product UI. The new Observation passed at exact commit `04e98710b1b60028152d833fa7c7ac180e85d0a7` with numeric repository, account and installation IDs recorded locally. The final-code rerun confirmed that the read-only App grant and the account's broader repository role are recorded separately. No App private key or client secret was generated, no repository writes were made, and prior chats/evidence remained. Live denial/revocation/expiry were not forced on the user's connection; those cases use controlled automated fixtures. App login expires and requires sign-in again; CI cannot inherit this live result or access these credentials.

### Earlier dashboard evidence (historical; behavior since changed)

The entries below verified dashboard iterations on 2026-09-04 that later passes replaced. Bulk selection and bulk verdicts, the floating selection toolbar, required reviewer name/reason fields, case-type grouping and per-answer archival no longer exist. [Reviewing saved output](#reviewing-saved-output-with-optional-judge-first-triage) describes the current behavior; these entries stay as dated evidence, not as a description of today's tool.

**2026-09-04 — one-click review and automatic judging:** 266 Vitest tests passed (the new dashboard tests cover reviewer-less verdicts and the automatic judge run after a live eval); the desktop dashboard journey passed (5.6 seconds) covering the single-step run and judge confirmations, one-click and keyboard verdicts with auto-advance, optional notes, bulk Pass/Fail, judging unjudged answers by default, archive/restore and the 16-answer queue. Lint and TypeScript passed. `tests/application/integration/journey-catalog.test.ts` fails only because two additional `@journey-dashboard` spec files were added concurrently outside this change. Synthetic saved data only; no live model calls.

**2026-09-04 — declutter pass on Eval runs and Run checks:** 263 Vitest tests in 21 files passed; the desktop dashboard journey passed (6.3 seconds) covering topbar page navigation, the single triage status line, sidebar filter counts, the run-level triage bar text, spot-check, judge/rerun confirmations, bulk verdicts, archive/restore and the 16-answer queue. Lint and TypeScript passed. Synthetic saved data only; no live model calls or mobile checks.

**2026-09-04 — separate eval route, single-case reruns and judge-first triage:** 263 Vitest tests passed; the focused desktop dashboard journey passed (6.1 seconds). Coverage includes direct `/evals` entry and reload, browser Back/Forward, draft/selection preservation across routes, one-repetition defaults, one-case rerun consent without judging, current-policy triage, hard-check precedence and human correction of a cleared answer. Lint, TypeScript and diff checks passed. The live dashboard was refreshed and visually checked; the existing sixteen-answer run and its review were unchanged. No live model calls or mobile testing; judge accuracy is not established by these synthetic checks.

**2026-09-04 — desktop visual consistency pass:** 250 Vitest tests passed; the focused dashboard browser journey passed after preserving the review redesign and correcting table-cell alignment, review-only bulk toolbar visibility, expanded page-scrolling answer lists, status alignment and long-title wrapping. Regression checks cover all five cells sharing a row boundary, retaining selection across tabs without showing review actions on Run checks, all sixteen answers without nested scrolling, and long labels staying inside their columns at 1100px desktop width. TypeScript, lint and diff checks passed. Existing saved user answers/reviews were unchanged; no live model calls or mobile testing.

**2026-09-04 — review queue redesign and Run checks refinements:** 250 Vitest tests in 20 files passed; the desktop dashboard journey passed (4.9 seconds) covering the answer queue with repetition groups, required reviewer name/verdict/reason, draft preservation and J/K navigation, LLM advice shown beside an unchanged human verdict, filtered select-all with the floating toolbar for bulk human verdicts and bulk LLM advice consent, failed archive requests keeping selection, archive/restore through the sidebar group, run isolation with unreviewable failed answers, and a sixteen-answer queue. ESLint and TypeScript passed. No live model calls; no server or API changes; existing user runs and reviews were not modified.

**2026-09-04 — run-level inbox correction and integrated review:** 250 Vitest tests passed; the focused desktop dashboard journey passed (5.0 seconds), including full-run selection, archive/restore across reload, isolation of old versus new runs, stale-request failure, and archived-run pending counts. A synthetic LLM pass is displayed beside a persisted human needs-discussion verdict without changing it. TypeScript, lint and diff checks passed. No live judge/eval calls or mobile tests. This supersedes the earlier case-type grouping and per-answer archival below. Existing user runs/reviews were not archived or changed during testing.

**2026-09-04 — modal pickers, eval-class selection and reversible archival:** 250 Vitest tests in 20 files passed; all nine desktop Playwright journeys passed (47.7 seconds). Dashboard coverage includes modal cancellation/focus, retained choices, class tri-state selection, direct bulk-judge consent/model changes, bulk human persistence, failed archive requests, empty inboxes, restore/reload and selection isolation across runs. Original results and reviews stay unchanged by archive operations; stale hashes and malformed/symlinked metadata are rejected. TypeScript, lint and diff checks passed. No new live model calls or mobile checks. Local success does not resolve the existing remote CI cold-navigation timeout.

Fable reviewed the earlier dashboard source and saved desktop screenshots, not a live browser. Adopted: modal pickers, visible bulk actions, class grouping, consolidated judge settings and less repeated page chrome. Kept archival as simple reversible hash-bound inbox metadata rather than adding another append-only event log; existing evidence/verdict histories remain authoritative. The layout detector ran with missing parser dependencies and fell back to limited regex checks; desktop visual inspection remains the relevant layout evidence.

**2026-09-04 — journey selection, explicit live-eval counts and bulk review:** 246 Vitest tests in 20 files passed; all nine desktop Playwright journeys passed (40.4 seconds), including synthetic selection/consent requests and actual bulk human-review persistence. TypeScript, lint and diff checks passed. The catalog parity test checks every selectable journey against Playwright's discovered tags. Judge selection/stop-on-first-failure tests use no model; real bulk LLM judging was not executed. Desktop screenshots cover the selectors and bulk form. Existing live results and human verdicts were not changed by verification.

**2026-09-04 — testing dashboard / checked-in runner follow-up, `codex/phase-one-evals`.** Selected desktop application scenarios are now executable under `tests/browser/`. Dashboard and judge safety tests use synthetic saved data; no paid judge run or new live baseline was started for this follow-up. The previous live baseline was moved intact into `tests/results/evals/`; its source fingerprints remain historical, and its human meaning review is still pending. Hosted CI results must be checked separately after the workflow runs.

- **237/237 Vitest tests** across 18 files passed; opening the dashboard and starting its Application tests control also completed successfully.
- **9/9 desktop Playwright scenarios** passed (eight application journeys plus dashboard review), 38.6 seconds. Includes draft preservation/focus between review cases, saved-review reload, explicit spend confirmation, exact application removal/recreation and disconnect/reuse on disposable state.
- TypeScript, lint and whitespace checks passed. An isolated production **webpack** build passed without disturbing the development server or its `.next` output. Hosted CI uses the normal build command and is a separate verification surface.
- Dashboard desktop captures were saved and inspected under `tests/results/dashboard-review/`. No mobile checks or new provider calls were made. The existing live baseline's `results.json` SHA-256 stayed `4cc302e04317f368ff6af44b17ce3447d09553e02cb51f17bf7fe8fed7c5de65` after relocation.

The dashboard is a local convenience over fixed runners and artifacts, not a product control plane. Fable's focused read-only architecture review supported this scope and highlighted explicit spend consent, same-origin controls, isolated judging, immutable source evidence and keeping CI smoke small. These boundaries are implemented and regression-tested; an isolated judge can still produce a biased or mistaken rating.

### Earlier evidence (historical, not a fresh full-contract pass)

**2026-09-04 — audit follow-up and first live baseline on the dirty `codex/pi-setup` checkout, based on `2f82308`. The complete contract is not passing yet.** The two confirmed audit defects are now fixed; the earlier three-agent journey evidence below is supplemented by targeted regression checks and a real-Pi run.

Fresh checks:

- Current deterministic Vitest suite: **223/223** tests passed across 15 files, including Origin checks on all mutation handlers, saved-setting preservation and tests of the eval checks themselves. These tests make no model calls.
- Fresh targeted checks: actual Next route rejects hostile-Origin `text/plain` setup POST without modifying saved configuration, accepts same-origin JSON; desktop Cancel and Escape both restore focus to Disconnect.
- Earlier three-agent audit: **44 HTTP/SQLite assertions**, **20 read-only HTTP assertions** against browser-created state and **15 extra disposable boundary tests** passed. These were not all rerun during the targeted fix pass; the current ordinary suite and targeted checks above were rerun.
- TypeScript, lint, whitespace checks and an isolated production build passed.
- First real-Pi baseline: **16/16 automated cases passed** (eight inputs, two repetitions), `gpt-5.6-sol` / `high`, Pi `0.84.4`, 84.26 seconds. Source fingerprints stayed unchanged. Human meaning review remains pending: both unresolved-conflict replies recommended a concrete compromise without recording a Decision, which needs interpretation against the initial rubric. The assistant's review is not human approval. Local artifacts now: `tests/results/evals/2026-09-04T08-42-41.046Z-Q6vAk1/`.
- Real production login start/poll/cancel routes shared the same attempt correctly. The invalid-model probe stopped before provider authentication. The earlier coordinator-sharing concern was not reproduced; this says nothing about restart recovery or multiple processes.

| Cases | Current evidence / status |
| --- | --- |
| P1-01, P1-03 | Discovery, consent UI, recovery and preferences passed in deterministic tests; the setup Origin defect is now fixed and regression-tested. The configured live model ran successfully; live quota/auth failure acceptance was not run. |
| P1-02 | **Partial:** synthetic fresh/replacement success and failed/cancelled replacement preservation passed; real provider rejection/expiry was not exercised. |
| P1-04–05, P1-07–08, P1-12–13 | Exercised deterministic browser/server paths **passed:** creation, isolation, rollback/retry, evidence, archival, double-send prevention, draft preservation and slow-create navigation. Pi/GitHub adapters were synthetic. |
| P1-06 | **Partial:** real-Pi proposal count, persistence/provenance/supersession passed on the eight-case baseline. Human meaning review remains open; the unresolved-conflict expectation was clarified on 2026-09-04 (suggesting a resolution is allowed, recording one is not), which applies to runs from that date on. |
| P1-09–10 | **Partial:** browser confirmation/cancellation and separate HTTP/domain deletion/disconnect passed, including fresh IDs, late-result rejection and preservation of other records/credentials. Final destructive browser clicks were not submitted. |
| P1-11 | **Partial:** Disconnect focus restoration is now fixed and both dismissal paths passed in the desktop browser. Application-removal restoration passed in the earlier audit. A full keyboard-only/screen-reader audit was not completed. |
| P1-14 | Drawer evidence/tabs, X/Escape/backdrop restoration and Ask Pi prefill/focus **passed**. No background application control received focus in the native modal traversal; full accessibility/focus-wrap verification remains partial. |
| P1-15–16 | Deterministic checks **passed:** visible 503 retry/recovery, terminal 404, late callback handling, detected/draft model settings, invalid combinations and real-adapter configuration boundaries. This is not live OAuth/model acceptance. |

Audit fixes and remaining work are tracked in the [retained acceptance record](../previous-direction/docs/plans/implementation-history.md#configure-pi-explicitly):

1. **Fixed: setup Origin bypass.** The earlier hostile-Origin `text/plain` POST could create or overwrite configuration. [`parseJsonRequest`](../../../src/server/schemas.ts) now guards every JSON mutation; bodyless archive/rerun/login-cancel handlers guard explicitly. All 11 mutation handlers have positive/negative regression coverage. This does not add authentication or claim the local app is safe to expose publicly. Browser delivery/exploitability of the original defect was **not** tested.
2. **Fixed: Disconnect focus loss.** [`ConfirmActionDialog`](../../../src/components/server-guy/confirm-action-dialog.tsx) restores its connected opener after unmount/close. Both Escape and Cancel returned focus to Disconnect in the targeted browser check. The checked-in Playwright smoke suite now covers cancellation focus restoration too.

Earlier audit screenshots were inspected inline without saved image artifacts; that was not a complete saved-screenshot visual audit. The new Playwright runner retains synthetic failure evidence under `tests/results/`. No mobile checks were made. The opt-in baseline used the existing Pi login for real model calls, not a fresh OAuth flow or GitHub access. Broader Pi behavior, explicit GitHub onboarding and durable-worker acceptance remain unverified or unimplemented. Historical temporary audit evidence remains under `/tmp/server-guy-phase1-audit-dBJnR5` if the machine has retained it.

Design provenance only: the retired A/B/C prototype source is preserved on local branch `codex/archive-pi-setup-prototypes-2026-09-04`, commit `449b59d10366b0cd816433508aa0c1b175b98230`. It is not the current implementation or current acceptance evidence.
