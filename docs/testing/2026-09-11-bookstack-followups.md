# BookStack follow-ups: plan and status

Historical implementation and proof report for merged PR #49. Its next actions and workflow rules describe that work; [Roadmap](../../ROADMAP.md) now owns delivery, and [operator design](../operator-design.md) supersedes its architectural priorities.

Working plan for implementing the [BookStack audit](2026-09-11-bookstack-audit.md) findings on branch `claude/pi-evidence-and-state`, based on main `0dba3ec` (PR #48). Updated as work lands; the PR description carries the final account.

Principle: support applications through declared requirements with the smallest set of reusable capabilities; Pi plans, investigates and recovers with native tools; the core executes, constrains and records.

## Status

| Area | State | Notes |
| --- | --- | --- |
| Preserve audit rig and evidence | done | `425e4e7`. Reproducible rig in `tests/rig/`; raw evidence under ignored `tests/results/bookstack-audit-2026-09-11/`. |
| P1 Pi investigates from any conversation | done | `a7f5bee`. Accepted in the Rig A replay with real Pi (below). |
| P2 Verification expresses behavior | done | `66e30a6`. Accepted in Rig A: Pi's correction release proved the owner's login and rejected the defaults with a recorded command check. |
| P3 State protection beyond managed PostgreSQL | done | `8a912e4`, `b69454e`, `cedbd23`: declared owners and dump procedures, unlabeled database owners, owner-image scope and rollback, runner without `app`, isolated dump restore with content fingerprints. Accepted on Rig B: MariaDB, uploads and config backed up through Pi; the product's restore test and an independent full-application restore both passed (below). |
| Enabling: generated secrets, public URL, registries, storage connection | done | `8381572`. |
| UI correctness | done | `1db37f3`: Deployment history from recorded attempts; no exploration bar, invented scenarios or simulated Check now in production views; stale claims removed. |
| Proofs | done | Rig A replay for P1 and P2. Rig B (Linux host container, TLS MinIO) for BookStack: deployment, backup, the product's restore test and an independent full-application restore. Generalization with Healthchecks: deployment and behavior checks verified; its backup failed because its container ignores SIGTERM (below). |

## Incident: Rig B erased the machine's amd64 emulation (15:45–16:02 UTC)

The first Rig B host container ran systemd as PID 1, privileged, with the engine's cgroup namespace. At boot, `systemd-binfmt` cleared `binfmt_misc`, a single table for the whole Docker Desktop VM. This removed Docker Desktop's Rosetta handler for amd64 binaries. Every amd64 container on the machine could no longer start new processes. For about 17 minutes, other agents' `sg-fe64777e-*` containers failed their health checks with `exec /usr/bin/curl: exec format error`; their running processes were not restarted or touched. Rig B's own first intake also failed because Pi's amd64 workspace could not start.

Remediation, nothing restarted: I stopped the rig host, then re-registered the VM's own Rosetta handler (`/run/rosetta/rosetta`, flags `OCF`) and the one other entry the VM declares in `/etc/binfmt.d`, from the VM's mount namespace. An amd64 container then ran `uname -m` as `x86_64`. The rig image now masks `systemd-binfmt` and the `binfmt_misc` mounts, and runs systemd in a private cgroup namespace. Rig B's first intake failure stays in its records as an environment failure.

## Current findings

- **Rig B, first BookStack deployment (actual Pi, local Docker, Linux host container).** Intake produced the new records unprompted:
  - MariaDB as the owner of its data, with `mariadb-dump`, restore and a `CHECKSUM TABLE` fingerprint;
  - a command check that the owner's administrator authenticates and the published defaults do not;
  - a generated `APP_KEY` and database passwords, so the card asked only for the admin email and password;
  - a one-shot initializer before the web service;
  - `${SERVER_GUY_PUBLIC_URL}`;
  - `lscr.io` images, which the old pinning refused.

  Execution failed three times. LinuxServer's MariaDB 11.8.8 spun at full CPU at "Installing MariaDB/MySQL system tables" under the rig's nested amd64 emulation, beyond the host script's fixed 120-second health wait. Pi inspected the runtime, cleared a corrupt `ibdata1` it found safe to remove, retried, and stopped with that explanation, saying a different database image needs the owner's approval. Under the same nesting, the official `mariadb:11.4.13` initializes in about 10 seconds, and the official 11.8 image Pi chose next in about 6. The stall is therefore specific to LinuxServer's MariaDB packaging on this emulated host, not to MariaDB 11.8.
- **Product gap:** a failed first deployment can only be retried within its approved effects. Once a server exists, cancelling is refused and `prepare_release` needs a verified or observed runtime, so the owner has no way to approve a different database image. The rig continued with a second application on the same host. First, the failed deployment's containers were stopped (a rig intervention) to free port 80. The new request told Pi that the LinuxServer MariaDB image stalled on this host.
- The host script's `--wait-timeout 120` is fixed. A legitimately slow first start, such as a large database initialization on a small host, cannot declare a longer one.
- **Rig intervention (my error):** the Rig B app copy predated `b69454e` and the UI commit. The second application's first recommendation was therefore prepared by old code: its override left `bookstack`, which owns only a files volume, unlabeled. I refreshed the rig, cancelled that recommendation through the product (no server existed) and asked Pi to prepare it again. The stale recommendation is kept as ignored evidence.
- **Generated sign-in password (fixed in `56d7cc7`).** The next recommendation generated `BOOKSTACK_ADMIN_PASSWORD`. Generated values are never shown, so the owner could never sign in, yet the command check would still pass because it receives the value. The approval card listed it under "Generated privately at approval, never shown", so the owner declined and asked again. The planner rule now says never to generate a value a person must know or type. The third recommendation asked the owner for both administrator values and generated only the application key and database passwords.
- **Rig B deployment (actual Pi, local Docker, Linux host container).** The third recommendation deployed. Its first attempt failed its HTTP check, which expected "Sign In" on BookStack's login page, where this release says "Log In". Pi then:
  - read the deployed configuration and fresh logs;
  - tried BookStack's upstream templates, but the rig's unauthenticated GitHub quota was exhausted;
  - read LinuxServer's init scripts;
  - corrected the check, under the same name, to the login form's `name="email"` field.

  The second attempt verified, within the approved effects: the HTTP check passed, and so did the command check proving the owner's login works and the defaults fail (exit 0, about 1 s). Nothing mechanical stops a correction from weakening a check's expected text; this one still identifies the login form. Owner-side corroboration through BookStack's own forms: the defaults do not authenticate, and the owner's login does. The owner then added a book, a page with a marker, an attachment and an image.
- **Rig B backups.** Asked in plain words to protect the data and prove a restore, Pi proposed a daily schedule keeping seven copies. The approval text came from the recorded capture plan: "Capture stops bookstack. mariadb keeps running to dump its data." After approval, the host received its timer, service, runner and the rig's CA drop-in. Pi does not continue a multi-step request after an approved change completes. The completion is recorded in Backups and history, and Pi reads it with its tools only when the owner writes again.
- **Rig B backup and restores.** The first backup stopped `bookstack` while `mariadb` ran Pi's dump; the source paused 4.4 s. The runner uploaded 83,927 bytes and verified the downloaded archive by size and SHA-256.
  - **Product restore test.** Pi proposed it after the backup. It verified the archive, all 153 files and the backup identity. It loaded the dump into a fresh instance with no network and required Pi's fingerprint to match the source's.
  - **Receipt bug.** The receipt the controller reads dropped `database-content`, because the runner's sanitized view did not list it. The runner, the receipt schema and the Backups fact now carry it. A dump restore is credited only when that check is recorded.
  - **Independent full-application restore (harness, `tests/rig/host/restore-check.py`).** It used only the downloaded archive: the inventory matched, and the dump loaded with the same fingerprint. The whole stack came up in 12 s as a separate project with no ports and internal networks. On that network, BookStack rejected the defaults and accepted the owner. It served the marker page and image, and the attachment and image bytes matched their original SHA-256.
- **Generalization: Healthchecks (actual Pi, Rig B).** It is a different stack: Django, on the managed PostgreSQL slot. Given the same kind of plain request, Pi recommended:
  - an owner-supplied superuser email and password, and a generated `SECRET_KEY` only;
  - `SITE_ROOT` from `${SERVER_GUY_PUBLIC_URL}`, with registration closed and debug off;
  - an HTTP check on the login page;
  - two command checks: the owner's credentials authenticate, and an HTTP ping is recorded and the alert worker processes it.

  It also declared its own dump procedure for the managed PostgreSQL volume, which the controller already dumps and restores. The product accepted both, so the same database would be captured twice. Since `d91ee27`, preparation returns that as retryable feedback; this run predates the fix. To free port 80 for this run, I stopped BookStack's containers once BookStack's evidence was complete.

  The first attempt passed the login page and the owner's login. Pi's ping-and-worker check used a Django field that does not exist. Pi corrected that check under its name and kept its assertions. The second attempt verified all three checks: the superuser authenticates, and an HTTP ping is recorded and the alert worker marks an overdue check down.

  The first Healthchecks backup failed with `source-stop-failed`. The container ignored SIGTERM for the whole 120-second grace period and was killed (exit 137), so the runner refused the copy and restarted the application. Asked to prove a restore, Pi advised retrying the backup. It had no way to know why the backup failed, because the operation recorded no reason. Since `99d490d`, the operation carries the recorded reason. The container's stop handling still needs a correction, such as a stop signal the image honours; Pi could propose one now that it can read the reason.
- **Opt-in Docker proof harness (fixed).** The scripted Pi session in `native-release.docker.test.ts` had no `subscribe`, which the planner has used for its journal since `a7f5bee`. The gated proof therefore failed with "session.subscribe is not a function". The fake session now matches the interface the planner uses.
- **Misleading read error (fixed).** While planning Healthchecks, Pi read upstream paths that do not exist. `read_repository` reported each one as "The repository is missing or this login cannot access it", although the repository and commit had resolved. It now returns `kind: "missing"` for such a path.
- **P1 replay (actual Pi, local Docker, stand-in GitHub/Hetzner/SSH).** The replay rig copied the audit's final records and used its still-running containers. In a new conversation, given only the symptom, Pi called `inspect_runtime`, read the deployed bootstrap under `.server-guy/current/`, read LinuxServer's upstream `init-custom-files` runner with `read_repository` to confirm custom init files run under Bash, identified the PHP-under-Bash failure, and proposed a correction with `prepare_release` at the deployed revision, with instructions that require proving the owner's login works and the defaults fail. It did not propose recreation. One intervention recreated the symptom: `tests/rig/bookstack/bookstack.mjs reset-default` returned the administrator to BookStack's published defaults through its own forms (the rollback had already restored the broken bootstrap).
- Compose 2.40.3 `up --wait` succeeds when a `service_completed_successfully` dependency exits 0 and fails when it exits non-zero, so the host script already distinguishes a completed one-shot from a crash; only verification wrongly required every container to keep running.
- `docker compose exec -e NAME` takes the value from the calling environment, so check inputs travel on SSH standard input, never on a command line.
- Rig B is required for backups: the installer uses systemd and the runner copies volume data from host paths, neither of which the macOS-shell transport provides.
- The existing S3 hostname rule accepts `s3.<name>.amazonaws.com`; Rig B serves MinIO under such a name inside the host container only, so no destination exception is needed.

## Second pass: one protection mechanism, recovery and full restoration

Reviewer priorities on head `e86ba9a`: unify managed PostgreSQL with declared owners, let Pi recover a failed first deployment on the same Application and host, restore and boot the whole application through the product, prove both with BookStack and Healthchecks, and close two correctness gaps (unknown command outcomes; ownership lost through corrections). Governing question: can Pi manage different applications through the same small set of useful tools?

### Decisions

- **One database mechanism.** A database volume is protected by its declared owner's dump procedure; the managed PostgreSQL slot keeps only what it uniquely provides (a generated password, the official image, image continuity) and gets a default procedure (`pg_dump` custom, `pg_restore`, a per-table row-hash fingerprint) when Pi declares none. The runner's PostgreSQL capture route and the standalone dump-restore container go; legacy `postgres` archives keep their restore reader. Facts project the default onto older records, so existing schedules and receipts stay readable.
- **Writers pause by evidence, not by assumption.** Capture stops the services that mount a captured volume read-write, dependents first; dump owners keep running; everything else is untouched. An empty pause list is valid. The runner reads volume paths from Docker, copies bound configuration from the definition, and refuses a foreign read-write attachment.
- **Corrections keep declarations.** A redeclared volume inherits owner, capture and procedure unless the declaration sets them; removing or moving an owner is a state change that needs the owner's decision for that service. The owner-image rule compares the image reference, so an unpinned managed image is covered too.
- **Unknown command outcomes hold.** A check runs detached on the host and writes its exit status and output beside the attempt's host result. Transport loss, a missing status or a client-side timeout leaves a durable hold on the deployment; nothing executes or verifies again until `reconcile_release` reads the host record, which turns it into a known pass, a known failure for correction, or an explicit unresolved hold. Commands run under the container's own `timeout` when it has one, so a timeout stops the command rather than orphaning it.
- **Failed first deployment, same host.** `prepare_release` on a failed first deployment continues it under the deployment's approval: an ordinary correction retries the deployment operation with Pi's instructions (no new approval); a state owner's image change proposes a release whose approval names that authority. Both finish the deployment as live on the same Application and host.
- **Restore boots the application.** The restore test loads files and dumps into an isolated Compose project on the host (no published ports, internal networks, fresh labeled volumes, no controller labels), brings the whole stack up, and the controller runs the recorded command checks plus any checks Pi chose for the restore inside the restored containers. Targets are derived by the controller, never read from a receipt. The host's own recovery tears the copy down. `restore-check.py` is retired.

### Status

| Work | State | Evidence |
| --- | --- | --- |
| Gap B: declarations survive corrections; owner changes need authority | done | `7f8121f`: `dataRecords` inherits owner, capture and procedure; `scopeDifferences` refuses a changed or removed owner without that owner's state change and compares image references. Regressions: two-release case in `native-release.test.ts`, ownership and managed-image rules in `release-scope.test.ts`. |
| Priority 1: managed PostgreSQL through the declared mechanism; pause by evidence | done | `7f8121f`: default procedure `managedDatabaseProcedure`, projected onto older records; capture plan v2 pauses writers of captured files only; the runner's PostgreSQL route, `oneShot`, `postgresVolume` and mount-list checks are gone. Python and unit tests cover an empty pause list, a paused writer beside a running reader, and foreign writers. Rig proof pending. |
| Gap A: unknown command outcome hold, durable host result, reconciliation, timeout | done | `7f8121f`: detached check with start and exit records, `commandPending` hold, `resolvePendingCommand`, in-container `timeout`. Proofs: `command-check-hold.test.ts` (real shell, stub docker, lost session, one mutation, no replay; unbounded timeout stays unknown), unit cases, release-level hold and retry in `application-releases.test.ts`. |
| Priority 2: recovery of a failed first deployment through Pi | done | `7f8121f`, `2ca978f`: `prepare_release` on a stopped first deployment retries the work that continues it with a recorded correction, or proposes a state-change release that finishes it; the successor passes the stopped operation's queue hold. Proven with real Pi on Rig B (below): the first BookStack deployment stopped on the LinuxServer MariaDB stall; given only the symptom, Pi read the operation and the host, proposed the MariaDB image change with its evidence, the owner approved it, a second cause surfaced, Pi continued without a new approval, and the same Application went live on the same host. |
| Priority 3: isolated boot and command checks in the restore test | done | `7f8121f`: runner `--test-restore <run> --keep` boots the archived stack as `sg-restore-<run8>`; the controller runs recorded and chosen checks inside it and recovers; `restoreChecks` on `propose_change`. Python and unit tests. Proved on Rig B with real Pi: BookStack's restore test booted the copy and passed seven command checks inside it (below). |
| Priority 4: BookStack and Healthchecks proofs with real Pi on Rig B | done | Rig B hosts `sg-rig-host` (rig `rigb2`, port 3398) and `sg-rig-host-hc` (rig `rigb3`, port 3399, HTTP on 8082). BookStack: recovery of the stopped first deployment, protection with three defects found and fixed by the proof (`d93ed38`, `1f68840`, `5afa574`), verified backup and a restore test with the owner's content proven inside the booted copy. Healthchecks: recovery of the stopped first deployment after a rig wiring error, protection with the image's own shutdown fault found by Pi from the recorded reason (`die-on-term`), two more product gaps fixed (`fa3862a`, `d486ccf`), verified backup and a restore test with the owner's check and ping proven inside the booted copy. |
| Docs: architecture, product, requirements, roadmap, testing index | done | architecture.md for every mechanism and limit; requirements, roadmap and the evidence index reconciled with the proofs. |

### Second-pass findings (Rig B, actual Pi, `2ca978f`)

- **BookStack recovery through Pi.** The request was the audit's original wording. Pi recommended LinuxServer's BookStack and MariaDB images, a one-shot initializer, MariaDB as the owner of its volume with a `mariadb-dump` procedure, and asked the owner only for the administrator email and password. The approved deployment stopped after two attempts: LinuxServer MariaDB `11.8.8-r0-ls230` spun at "Installing MariaDB/MySQL system tables" under the rig's nested amd64 emulation, and the release session stopped, saying a different image needs the owner's approval. The owner wrote only "The deployment stopped and BookStack is not running. Please find out what went wrong and get it running on this server." Pi inspected the host and the failed operation, and proposed a release continuing the first deployment with `stateChange` for `mariadb` (official MariaDB 11.4 LTS on a fresh `mariadb-data` volume, the incomplete `mariadb-config` volume kept mounted because the scope requires existing volumes to stay). The approval card carried that assessment. That release fixed the database (11.4.13 healthy, migrations run, administrator created) but used its three attempts on a second problem: the initializer reset `/config` ownership after BookStack started, so BookStack went unhealthy. The owner wrote "It stopped again and BookStack still is not running. Please continue and get it working on this server." Pi found the ownership cause from fresh evidence and continued: `prepare_release` retried the failed continuing release under its approval with the correction recorded, no new approval was asked, and the deployment verified on the same host with five checks (login page, credentialed MariaDB access, migrations complete, owner administrator signs in and defaults are rejected, sustained health). Seven attempts in all, two operations approved by the owner (the deployment and the image change). No controller record was edited, no container was repaired by hand, no replacement Application was created, and Pi was never told the diagnosis. Owner-side corroboration through BookStack's own forms: the published defaults do not authenticate, the owner's login does; a book, a marked page, an attachment and an image were created.
- **Defect found by the proof (fixed in `d93ed38`).** Asked to protect the data, Pi proposed the schedule and the owner approved it, but the operation queued behind the original failed deployment operation: that operation kept its unknown-effects queue hold after the release that continued it had verified. Pi reported the queue state honestly rather than working around it ("still queued behind the earlier failed deployment record, despite BookStack now being live"). A resolved operation now releases its hold and never blocks the queue; the regression sits in `initial-release.test.ts`.
- **BookStack protection through Pi.** Asked to protect the data and prove restorability, Pi proposed the daily schedule (the approval text, from the recorded plan: "Capture stops bookstack. mariadb keeps running to dump its data."), then the first backup. That backup failed on the host with the runner's bounded `capture-failed`. Given only "The backup failed. Please find out why and make the backups work.", Pi read the operation, the deployed configuration and fresh logs, and found the cause: the obsolete `mariadb-config` volume, kept from the LinuxServer image, still carried a dump procedure that reads `MYSQL_*` variables the official image does not define. Its first correction reclassified that volume as files. The executor refused it, because an owned volume's declaration is that owner's state change, and Pi reported the authorization conflict instead of working around it; nothing was applied. One rule was too blunt: a change of an owned volume's recorded kind was refused even under the owner's state change, and its message did not say what to propose. `44690f0` makes the owner's state change cover the volume's kind, owner and capture, and the refusal names the state change to propose. Pi then asked the owner to cancel the refused release and re-proposed with `stateChange` for `mariadb` and its evidence; the owner approved and the reclassification verified. Asked for the backup, Pi met the plan's next honest refusal, "Service mariadb keeps running to dump its database but also writes volume mariadb-config. One service cannot both pause and dump", and proposed retiring the obsolete volume from MariaDB's mounts under the same authority, its data left on the host. `ff9f7fc` lets the owner's state change cover that too (the mount rule had refused it even with the approval), and the approval text now names data declarations and mounts beside the image.
- **BookStack protection, continued (`1f68840`, `5afa574`).** After the detach release the schedule no longer matched the deployment; asked to reconfigure it, Pi proposed the refresh and the owner approved it. The next backup failed again with `capture-failed`, and the runner of that era recorded no reason. Given the symptom alone, Pi guessed: it blamed the restricted database account and moved the dump to the root credential over the socket. The owner approved that guess as any owner would; it changed nothing, because the failing command was the verify procedure, whose fingerprint query named BookStack's old `books`, `chapters` and `pages` tables while this BookStack version keeps its content in unified `entities` tables. The product could not show that, so `1f68840` makes the runner record, for a failed declared procedure, which step failed, the exit code and the tail of what it printed, with every value of the owner's environment replaced by its variable name; the operation and the Backups view carry it, and the codes stay bounded. With that record ("the verify procedure declared for mariadb exited 1: … Table '$MARIADB_USER.books' doesn't exist"), Pi named the real cause, proposed a schema-aware fingerprint that enumerates the current tables with row counts and checksums, refreshed the schedule and took the first verified BookStack backup (201 KB, writers paused 4.9 s). Asked to prove the restore with the owner's page marker, attachment and image hashes, Pi chose three command checks (a PHP query through BookStack's own models, an attachment hash, an image hash). The product restored the archive, loaded the dump into a fresh MariaDB, matched the fingerprint, booted the copy in 12.8 s and passed the four recorded checks inside it; the first chosen check failed only because the owner's message had miswritten the book's name ("Server Guy audit handbook" for "Server Guy Audit Handbook"), which Pi's exact comparison reported honestly. The second restore test exposed a real defect: the run's receipt stayed flagged for cleanup after the kept copy was removed, because host recovery tried to abandon multipart parts of an already completed object and the storage call failed outside the unit's environment, so every later run was refused with "Resolve the outstanding backup cleanup" and the product had no path out. `5afa574` skips that storage step for a verified archive and makes the controller run the host's own recovery once before refusing; both have regressions. The runner update reached the host through a schedule reinstall the owner asked Pi for.
- **BookStack restore proof (`57646cad`, Rig B, real Pi).** After the runner reinstall the owner clicked Retry on the refused restore card: the controller ran the host's recovery, the flag cleared and the test ran. Pi's own checks then failed twice more on its path assumptions inside the LinuxServer image (it hashed `/config/www/files/uploads/files/…` where BookStack stores `uploads/files/…` relative to `/config/www/files`, and `/config/www/images/…` where gallery images live under `/config/www/uploads/images/…`); each time the operation recorded the check's exit and output ("No such file or directory …", "Expected one restored gallery image, found 3" with the three paths), Pi asked the owner to cancel the failed card rather than retry it, and re-proposed a corrected test. The fourth restore test verified: archive downloaded and matched, 634 files matched the inventory, the dump loaded into a fresh MariaDB and matched the fingerprint, the copy booted in isolation in 12.9 s (2 running, the initializer finished), and seven command checks passed inside it: the four recorded at deployment (credentialed query, migrations complete, owner signs in and defaults are rejected, sustained health) and the three Pi chose for this proof (page marker through BookStack's models, attachment 1 linked and byte-identical, the original gallery image byte-identical). The copy was removed and the host reports no pending cleanup. Owner-side corroboration on the live instance afterwards: defaults rejected, owner login works, marker, attachment hash and image hash unchanged. Records: `tests/results/rig/rigb2/evidence/bookstack-records-after-protection.json`, `bookstack-host-receipts.json`, `bookstack-restore-proof.json` (ignored, local).
- **Healthchecks first deployment (Rig B, second host, real Pi).** To run beside BookStack, a second host container (`sg-rig-host-hc`, HTTP published on local port 8082) got its own rig (`rigb3`, port 3399, MinIO of its own). Given the same plain request as in the first pass, Pi recommended LinuxServer's Healthchecks on the managed PostgreSQL slot with its own dump procedure declared for that volume, the owner's email and password as the only private inputs plus a secret key, an HTTP criterion on the login page and command checks for documentation, the owner's login and the alert worker. The approved deployment stopped after three attempts with `Public application check rejected (HTTP 404)`. Asked only "The deployment stopped and says it could not be completed. Please find out what went wrong and get Healthchecks working on this server.", Pi inspected the host, found both containers healthy with zero restarts, blamed its hard-coded login path and resumed the deployment under the existing approval with no new approval asked (the Priority 2 path), which then stopped twice more: first on a readiness path answering 302, then with 404 again, which Pi correctly reported as rejected "before requests reached the Healthchecks container". That was the rig, not the product or Pi: the provider stand-in records 127.0.0.1 for every rig, so the executor's public checks went to the BookStack host on port 80 while Healthchecks listened on 8082 (BookStack has no `/accounts/login/`; the first 404 was BookStack answering). Two product observations came out of it anyway: the executor swallowed a redirecting health path as a connection failure and retried it for the whole window before saying only "did not become ready", so `d486ccf` treats a redirect as a response, rejects it with its status and target, and names the last response seen when readiness times out. The rig fix (`5a248e0`, `3c6e680`): one host at a time owns the stand-in address; BookStack's host was stopped after its proof and a forward container publishes 127.0.0.1:80 to the Healthchecks host. With the rig corrected, the same wording again ("It stopped again and Healthchecks still is not working. Please continue and get it working on this server.") led Pi to isolate the fault to public routing ("release checks received 404 before reaching Healthchecks, while host-side requests returned 200"), resume once more under the existing approval, and the deployment verified: readiness of both services, the login page, and the command checks that the owner's credentials sign in and that the alert worker processes an overdue marked check. Three owner messages, one approval (the original deployment), no controller record edited, no container repaired by hand, and Pi was never told the diagnosis; the rig's wiring, not the product, cost the middle rounds.
- **Healthchecks protection (Rig B, real Pi, `cf73767`).** The owner created a check with a marker name and pinged it through Healthchecks' own forms, then asked for daily backups keeping a week of copies. Pi proposed the schedule (the plan: pause `healthchecks`, dump PostgreSQL through its declared procedure) and the backup. The backup failed with `source-stop-failed`: the container ignored the stop request for the whole grace period and was killed, as in the first pass. Given "The backup failed. Please find out why and make the backups work.", Pi read the recorded reason and proposed a 30-second `stop_grace_period`, which the runner's own two-minute stop overrides; the schedule had to be rebound after the stack changed (Pi proposed it, the owner approved) and the retried backup failed again, now with the runner's new record "Service healthchecks exited 137 after the 120-second stop grace period (killed)" (`cf73767`). Pi then set the stop signal explicitly and bounded s6's shutdown; same result. Its third proposal changed the protection plan instead: keep the config volume but stop capturing it, since Healthchecks' checks, pings, projects and settings live in PostgreSQL. The executor refused that as an ordinary correction, because dropping a capture from an owned volume is the owner's state change, and Pi reported the refusal and asked for the owner's authorization. A throwaway experiment on the rig host with the pinned image (own network, own PostgreSQL, no volumes, `healthchecks-stop-experiment.sh`) showed the same behavior for both SIGTERM and SIGINT, with no s6 shutdown output at all, so no Compose-level shutdown correction can succeed on this rig for this image; the native arm64 build of the same image behaved the same with uWSGI running (killed after the full timeout) and stopped in 4 s only when uWSGI had not started, so this is the image's shutdown behavior, not the rig's emulation (`healthchecks-stop-experiment-arm64*.log`). One more product gap came out of Pi's honest report: the refusal told Pi to propose the capture change "as a state change for healthchecks", but `prepare_release` accepted only database owners in `stateChange`, so the decision path the product named did not exist; Pi said so ("the release API cannot scope that state authorization because it does not recognize the files-volume owner") and fell back to a shutdown wrapper. `fa3862a` lets a state change name any declared owner; the image-continuity rule still applies to database owners only. Pi's wrapper release verified without the wrapper (it reported the wrapper incompatible with s6-overlay and rolled it back within its attempts), and its next investigation found the actual cause in the packaged uWSGI configuration, which lacks `die-on-term`, so SIGTERM reloads uWSGI instead of ending it. Its release added that setting, verified with a direct check of the generated configuration, and after the schedule rebind the next backup got past the stop for the first time: the capture then failed in Pi's own PostgreSQL verify procedure, recorded as `relation "checks_check" does not exist` (Healthchecks' tables are named otherwise), the same kind of record that let it correct the BookStack fingerprint. Pi replaced the query with a fingerprint of every application table, rebound the schedule, and the retried backup verified: 23.9 KB uploaded, downloaded and matched, the source paused 10.0 s with Healthchecks now stopping cleanly. Asked to prove the restore with the owner's marker check and its ping, Pi proposed the test with its own checks; the product restored the archive, loaded the dump into a fresh PostgreSQL and matched the fingerprint, booted the copy in 6.3 s (2 of 2 services) and passed the three recorded checks inside it (the uWSGI configuration, the owner's login, the alert worker); Pi's own check failed on a Django attribute name (`'Check' object has no attribute 'pings'`), recorded verbatim, and was corrected in the next round: Pi asked the owner to cancel the failed card, re-proposed with the Ping relation read from Django's model metadata, and the restore test verified: five command checks passed inside the booted copy (the uWSGI configuration, the owner's login, the alert worker, the named audit check with its ping history, and Healthchecks serving the restored database); the copy was removed. Owner-side corroboration afterwards: the marker check and its one ping are still on the live instance. Records: `tests/results/rig/rigb3/evidence/` (`healthchecks-records-after-restore.json`, `healthchecks-host-receipts.json`, `healthchecks-restore-proof.json`, the stop experiments).
- **Rig note: emulation.** The product pins every image to its Linux amd64 digest, because the hosts it targets are amd64; the rig host containers are aarch64, so inside them every application container runs under Rosetta. That is a rig limitation to keep in mind for stop behavior and timing (BookStack's containers stopped in seconds under it; the Healthchecks image did not stop at all), not a product property.
- **Rig note.** Docker Hub no longer serves MinIO's `latest` tag; the rig host pulls MinIO from Quay. On this machine the outer Docker Desktop engine's pulls hang in its credential helper (`credsStore: desktop`); the opt-in Docker proof runs with `DOCKER_CONFIG` pointing at a config that holds only the CLI plugins. The rig host's own dockerd is unaffected.

### Tests and proofs on the final candidate

Head `fa3862a` (docs commits follow it and change no code): TypeScript suites 846 passed, 8 skipped (`tests/results/rig/rigb2/evidence/logs/vitest-full-fa3862a.log`); Python operator suite 87 OK (`python-operator-fa3862a.log`); the opt-in Docker proof 3 passed, 3 Pi-proof cases skipped without `SG_RUN_PI_PROOF` (`docker-proof-fa3862a.log`). Focused regressions added in this pass: `command-check-hold.test.ts` and the initial-release/application-releases cases for the queue hold, the stopped-first-deployment continuation, the files-owner state change; `scheduled-backup-operations.test.ts` for recovery before refusal; `scheduled-backup-facts.test.ts` and the Python suite for procedure and stop detail; `deployment-executor.test.ts` for the readiness report. The Rig B runs above were made against the code as each defect was fixed; the rig's copy was refreshed and its worker restarted between Pi steps, and runner changes reached the hosts through schedule reinstalls the owner asked Pi for.

### Limits and interventions

- No real provider: Hetzner, GitHub and SSH are stand-ins; S3 is TLS MinIO inside the host container. The hosts are aarch64 containers, so the product's amd64-pinned images ran under Rosetta.
- The Healthchecks first deployment's middle rounds were lost to the rig (public checks reached the other host); the rig now forwards the stand-in address to one host at a time.
- Interventions, all owner-side or rig-side: the owner cancelled failed cards when Pi asked; the owner clarified a miswritten book name; the owner granted the decision Pi asked for on the config-volume capture (Pi then chose the `die-on-term` correction instead); the owner asked Pi to reinstall a schedule after a Server Guy update; the owner removed a duplicate check it had created through Healthchecks' own form; the rig's app copy was refreshed and restarted; a port forward container was added. No controller record was edited, no container was repaired by hand, no replacement Application was created, and Pi was never told a diagnosis.
- Pi needed several rounds for its own checks and procedures (paths inside images, table names, a Django relation); each failure was recorded with the check's output and Pi corrected it from the record. The number of rounds is a cost of this design, not hidden by it.
- Still not shown: capture and isolated restoration of the independently built shared-state topology; legacy schedules on a real host; an application without a public HTTP endpoint; a controller update refreshing installed schedules on its own (today the owner asks for a reinstall).

## Third pass: replay, successor operations and consistency boundaries

Reviewer gaps on head `3912c9c` (code `fa3862a`), accepted direction unchanged.

| Gap | Change | Proof |
| --- | --- | --- |
| 1. Reconciliation replayed completed mutating checks: a known successful command cleared the hold and `verifyRelease` ran every check again. | Verification resumes from the prior attempt's receipts: HTTP checks recorded passed are carried over, commands recorded passed and the resolved command are consumed, only unfinished commands run. | `command-check-hold.test.ts` runs `reconcileRelease` to completion with two commands: one mutation after recovery, not two. |
| 2. A Pi-created continuation was mistaken for owner acceptance: any other operation id cleared `commandPending`. | The hold is a fact of the deployment, not of an operation. Only an owner decision made through a card (Retry, or approving a proposal whose text names the unknown) marks the hold accepted; `reconcile_release` then proceeds only when the host's record is lost, never while the command runs. Pi's `prepare_release` continuation keeps the hold. | `initial-release.test.ts`: `prepare_release` alone cannot clear the hold; the owner's Retry can, and a still-running command holds even then. |
| 3. Writers were inferred from mounts only; a network client could write between the dump and its live fingerprint. | Data records may declare `writers`: services that change a volume's data without mounting it. They pause for the capture. A dump whose writers Pi declared (possibly none) is quiescent and its live fingerprint counts; without a declaration the dump is online by the tool's own snapshot, the runner takes no live fingerprint, and the restore test proves it by restoration, not comparison. Mounts stay evidence for file writers. | `native-release.test.ts` (plan), `test_scheduled_backups.py`: a network writer pauses when declared; an undeclared one leaves an online dump with no live comparison and an honest restore record. |

### Third-pass results

- **Gap 1, resumed verification (`2ef383f`).** `reconcileRelease` finds the held command's own attempt, reads the host's receipt of the replacement and the command's record, and verifies again from what that attempt established: HTTP checks it recorded passed are carried as receipts (a create among them already made and removed its object), commands recorded passed and the resolved command are consumed, and only unfinished commands run. `command-check-hold.test.ts` now runs reconciliation to completion under a later operation with two commands: the mutation happened once before the reply was lost, the second command ran once, the resolved receipt is recorded once. `deployment-executor.test.ts` shows the HTTP receipts carried with no request sent beyond readiness. Removed: the second execution of every check after a known pass.
- **Gap 2, the hold across operations (`2ef383f`).** The private `acceptUnknownCommand` that treated any other operation id as the owner's acceptance is deleted. The hold stays on the deployment until the host's record resolves it; a release operation reads that record before anything runs (`runApplicationRelease`, `runInitialRelease`), and Pi's `prepare_release` continuation keeps it. The owner's decision is recorded where it is made, on the deployment card's Retry and on a release card's approve or retry, as `acceptedAt` on the hold; reconciliation then proceeds only when the record is lost, never while the command runs. `initial-release.test.ts`: `prepare_release` alone cannot clear the hold (nothing executes, no acceptance event); the owner's Retry through the route records the decision but a still-running command holds; once the record is lost the command runs again, once. Removed: acceptance inferred from operation identity.
- **Gap 3, the consistency boundary (`7c7f9e0`).** Data records may declare `writers`; the planner rules explain when to declare them and what an empty list asserts. The capture plan pauses mounters and declared writers, dependents first, and marks each dump `quiescent` only with a declaration or when nothing else keeps running; the runner takes a live fingerprint only then, records `fingerprint: null` otherwise, and the restore test proves an online dump by loading it (`database-restored` without `database-content`), which the operation evidence and the approval text state. `native-release.test.ts` covers declared writers pausing without a mount, the empty declaration, the online default, the owner as its own writer, and the all-paused case; `test_scheduled_backups.py` covers the online dump end to end (no live fingerprint at capture, no comparison at restore, the copy loaded). The BookStack and Healthchecks proofs of the second pass remain valid under the new rule: in both stacks every service other than the dump owner paused for captured files, so their dumps were quiescent by evidence. Removed: the assumption that mounts define every writer.
- **Tests on head `7c7f9e0`:** TypeScript suites 850 passed, 8 skipped; Python operator suite 88 OK; opt-in Docker proof 3 passed, 3 Pi-proof cases skipped (`tests/results/rig/rigb2/evidence/logs/*-7c7f9e0.log`). No rig run this pass; the BookStack and Healthchecks evidence of the second pass is retained.
- **Review corrections (`6a1dccc`).** `dataRecords` had validated and merged `writers` but dropped them from the record it returned, so no declaration reached the saved configuration; it now persists them, an empty list distinct from an absence, inherited through plain corrections, proved through preparation, `nativeFacts` and `backupCapturePlan` with a declared network writer entering the pause set. The product's restore fact required `database-content` for every dump; the schedule now records whether any dump was compared, loading is required for every dump and a match only for compared ones (older schedules keep requiring the match), and the wording names which claim the receipt supports. TypeScript suites on `6a1dccc`: 852 passed, 8 skipped; Python operator suite 88 OK.
- **Not solved by stopping everything or by a dependency framework:** the pause set is still derived from declared evidence per volume; there is no service graph beyond Compose's `depends_on` ordering, which already existed.

## Next actions

The final account is in PR #49. The smallest next step: prove the same protection and restoration on the independently built shared-state topology (web and worker sharing files with SQLite), whose capture pauses two writers of one volume, and let the restore test's chosen checks run against it.
