# Testing and evidence

Hallvi was called Server Guy, then Haldur, before it was named Hallvi on 18 September 2026. Dated records here keep the name, commands, paths and labels that existed when they were written; current names are in [Installing Hallvi](../installation.md).

For local or cloud fixtures, follow the [development resource policy](../development-resources.md).

[Requirements](../requirements.md) define user outcomes; [Roadmap](../../ROADMAP.md) distinguishes shipped work from gaps. [Test runners](../../tests/README.md) owns commands, fixtures and opt-in Docker/model runs. The small [acceptance guide](phase-one-acceptance.md) stays at its existing path because the local test dashboard loads it.

## Current redesign verification

[Operator design](../operator-design.md#verification-during-development) and [Roadmap](../../ROADMAP.md) define proportionate verification and stage order. Prove a lightweight deployment, then medium and more complicated examples through the same operator. Exact repositories remain to be selected. Each stage needs focused checks and a concrete reviewable result; old workflows and obsolete hardening tests may be deleted.

Redesign checkpoints so far, newest first: [Pi owns the conversation, the worker owns Pi's sessions](2026-09-19-pi-owns-conversations.md) (no Hallvi message or reply records, no locks or start gates; real provider, approval, Stop and a killed worker with explicit Continue on a real host), [conversations on Pi's AgentHarness](2026-09-19-agent-harness-migration.md) (real provider, workspace and host; restart contract; upgrade and rollback on copies of real data), [Overview, live, on a real server](2026-09-19-overview-live.md) (Pi put Caddy in front of a private linkding on a rented server, turned on access logging and recorded it twice over; both log sources followed through SSH, SSE and the real page against the server's own log, 19 September 2026 — [PR #151](https://github.com/lustoykov/hallvi/pull/151)), [application-first Backups](2026-09-17-backups-application-first.md) (the application's tangible data, host-to-copy hierarchy, operational failure and compact controller recovery, 17 September 2026 — isolated record scenarios, no provider call), [diagrams that hold their labels](2026-09-15-diagrams.md) (the Architecture map and the Overview thumbnail on a Paperless-shaped stack, and the launch-path typography question answered with no CSS change, 15 September 2026 — merged in [PR #86](https://github.com/lustoykov/hallvi/pull/86) as `6177177`), [recovery correctness and truthful backup facts](2026-09-15-recovery-correctness.md) (the secret store in the recovery archive, a credential change that can end unresolved, and a Backups verdict that reads copies rather than intentions, 15 September 2026 — merged in [PR #84](https://github.com/lustoykov/hallvi/pull/84) as `10646dd`; local stand-in for the bucket), [publishing at the owner's own domain](2026-09-15-publish-custom-domain.md) (Ghost deployed privately, published at an authorized hostname with a real certificate, used, broken, recovered and made private again on real Hetzner hardware, 15 September 2026 — merged in [PR #76](https://github.com/lustoykov/hallvi/pull/76) as `4941350`), [Hallvi's own protection](2026-09-15-controller-protection.md) (automatic encrypted copies of the controller's own records and keys, with a recovery kit and a verified reopen, 15 September 2026 — local stand-in for the bucket, no real provider call), [four real applications through the product](2026-09-13-deployment-capability.md) (Docker Getting Started, Paperless-ngx, Ghost and Plausible CE deployed, used, updated, broken and recovered on real Hetzner hardware, 13 September 2026 — merged in [PR #68](https://github.com/lustoykov/hallvi/pull/68) as `e6fc479`), [the simple deployment journey end to end](2026-09-13-simple-journey.md) (Docker Getting Started opened and used through the product's own access link, 13 September 2026), [Architecture from real records](2026-09-12-architecture-records.md) (the first designed destination driven by records Pi wrote, accepted 12 September 2026), [transcript and terminal](2026-09-12-transcript-terminal-integration.md), [Hetzner provisioning](2026-09-12-hetzner-provisioning.md), [operator storage](2026-09-12-operator-storage.md) and [operator execution](2026-09-12-operator-execution.md).

The redesign has not been implemented or validated by the historical proofs below. Documentation/link checks establish documentation consistency only. Detailed view capabilities, application-error detection and broad resilience matrices are deferred until the main deployment journey works.

## What the existing architecture has proved

At main `0682ab2` (PR #45), the actual configured Pi model (`openai-codex` / `gpt-5.6-sol`, high effort) installed and updated two materially different applications through the shared native path:

- Notes with PostgreSQL/private input: Pi corrected real host build failures and retained data/private configuration through the update.
- Independently built web/worker services with shared files and SQLite: both images changed, all three volume identities and stored data survived, the v2 worker processed the retained document, and read-only writes were refused. Pi corrected an unsuitable intake check and anticipated a UID/retained-volume permission problem before execution. This second proof required no production changes.

The model, workspace, resolver, host script, records and verification were real. The provider was fake, SSH mapped to local execution, repository trees were fixtures and host HTTP was mapped to loopback. Worker processing in the second proof was asserted by the harness; the product checked image identity, readiness and HTTP behavior. That application did not demonstrate correction from host feedback; the notes application did. Neither establishes universal Compose support or every app's rollback/backup behavior.

The final proof passed once in 215.6 seconds. Its default suite had 1,006 passing tests and 26 opt-in skips; Codex separately passed TypeScript and a scripted first-deployment Docker regression. These are dated results, not today's suite status. The historical proof code is preserved in [native-release.docker.test.ts at the pre-cleanup revision](https://github.com/lustoykov/hallvi/blob/0682ab257469bc5cee994572285283ea949bc3c6/tests/application/integration/native-release.docker.test.ts).

## Dated evidence

Keep this concise index rather than one permanent working-tree report per development session. Each link identifies the original committed account, with exact revisions, commands, failures and limitations. A historical record is not a current uptime or compatibility claim.

| Proof | Established result and boundary |
| --- | --- |
| [Four retained applications, one owner each, 22 September](2026-09-22-retained-state-separation.md) | The four-application development state was separated into one state directory per application with a verified copy first, each database reduced to its own application and checked against the source counts, the original kept read-only and marked so no program opens it. Two checkouts then held different applications at once; a second attach, a stray `db()`, `npm run dev` and a migration against an owned directory were refused; a crash was accounted for; a real Pi turn on whoami survived a detach and an attach from the designated checkout with its full history; all four applications' records and host data were intact afterwards. A review then found four faults — an old process writing under a new owner, one Ctrl-C read as two, an unreadable worker read as idle, snapshots without histories — each fixed, tested and re-run live on synthetic state. One application per checkout (`next dev` allows one server per directory); the host stays shared. |
| [Automatic deployment from GitHub, 22 September](2026-09-22-automatic-deployment.md) | Through the real setup journey on a real Hetzner host: Pi asked how the application should deploy, the owner chose *automatically when main changes* in one card, and five later pushes to a private repository reached the running application with no chat message — noticed within a minute each, deployed as exact commits, data kept. Pause held a push for 2½ minutes and Resume deployed it; a commit pushed while Hallvi was stopped was found on the first look after restart; a commit whose health endpoint answered 500 was rolled back, recorded as failed with the reason, offered a Retry and never retried by itself; the next push recovered. Not exercised live: a worker stopped mid-deployment, two tracked applications at once, Always ask, and an installed service on the Mac mini. |
| [One-script install and hourly checks, 20 September](2026-09-20-one-script-install-and-hourly-checks.md) | On a disposable Ubuntu host, `sh install-hallvi.sh` with no arguments found a signed stand-in release over HTTPS, verified its signature before unpacking, checked the archive against the manifest and installed it. The installed controller then found a release 50s after starting and a newer one 70s after it was published, with no browser open and no button pressed, and installed neither; an untrusted manifest left the previous answer in place with the reason. The dashboard's packaging check passed against the real 64,968-entry archive after three false positives were corrected. No release was published and the publish button was not pressed. |
| [Schema migrations, 20 September](2026-09-20-schema-migrations.md) | One migration list read by both `npm run db:upgrade` and the installer the updater runs. On a disposable Ubuntu host, a real installed release A (schema 15) was upgraded to release B (schema 18) with its application, conversation, messages, saved facts and pre-upgrade history intact, the verified copy kept and named. With the new version unable to start, the installer put both the records and the old program back and the old one served again. Installing the old release over migrated records was refused without replacing anything. A review found four faults, all fixed and re-proved on a second host: eligibility now comes from the release's signed `migratesFrom` rather than the installed program's own list, the installer stops and confirms writers are gone before restoring, and restoring remembers the exact database and verifies the copy before touching anything. The updater's own download path was still not exercised across a schema change — no two signed releases with different schemas exist yet — and no conversation was opened on that host. |
| [Persistent development environment, 20 September](2026-09-20-persistent-development-environment.md) | One Hallvi kept between tasks on the owner's MacBook, with four applications really deployed by real Pi on one shared Hetzner cx23: a stateless site, Uptime Kuma on its own SQLite, Miniflux with a separate PostgreSQL, and Paperless with PostgreSQL, Redis, workers and documents. Retained state from 18 September was adopted rather than remade, carried from schema 15 to 18 with a verified backup, and its pre-upgrade conversations imported without touching the originals; the applications, their history and their sample data survived. A schema-18 program refused a schema-19 database without writing. Not monitored, and no deletion, failure or recovery was tested against it — that stays in disposable fixtures. |
| [Downloads and updates, 20 September](2026-09-20-downloads-and-updates.md) | On a disposable Ubuntu 24.04 installation with a real systemd user service: a first download and install, A→B updates from both `hallvi update` and Settings through an SSH tunnel, an unsent draft and all records preserved, ports kept, a stopped installation left stopped, rollback when the new version could not start, two updates at once refused, and four releases refused for an untrusted signature, a wrong platform, an incompatible schema and altered bytes. On macOS: both archives built, and the update helper's launchd lifetime proved with the real code, but **no installed-service update** — the owner's own Hallvi was not touched. No release has been published and the release workflow has never run. |
| [Integrated prebuilt install, 19 September](2026-09-19-integrated-prebuilt-installation.md) | Exact optional-Docker candidate installed on Apple-silicon macOS and clean Ubuntu 24.04 without local Node/npm/Docker; laptop browser, service restart, Ubuntu reboot and same-archive reinstall passed. The owner's fresh ChatGPT connection drove real public-repository inspection, private deployment to a separate Hetzner host, and application use after controller restart. External-user and private-GitHub acceptance remain open. |
| [Optional local Docker, 19 September](2026-09-19-optional-local-docker.md) | With the controller unable to reach Docker, real Pi inspected a real repository in the direct workspace, wrote its packaging there, and deployed it privately to a real Hetzner CX23 that served and persisted data; a Docker choice that could not be met withdrew the workspace with its reason. Docker mode passed against a real engine. Development controller on macOS, not an installed service. |
| [Beta candidate, 18 September](2026-09-18-beta-rehearsal.md) | Clean Linux install, state-preserving upgrades and restart, schema refusal, crash recovery and reboot; native ARM/x64 repository workspaces; real private linkding deployment. Existing model account reused; external-user acceptance remains open. |
| [Installed service, 17 September](2026-09-17-installation.md) | Real macOS and real Hetzner Ubuntu installations of the packaged background service with the owner's real model account: install without root, real Pi operations through the service-managed worker, crash restart under launchd and systemd, boot without login on Linux, upgrade and uninstall keeping state, and a private application link and the browser terminal used from a laptop through the printed SSH configuration. No macOS logout/login, arm64 Linux, schema-changing upgrade or workspace on the virtual machine. |
| [BookStack follow-ups, 11 September](2026-09-11-bookstack-followups.md) | Proofs with actual Pi and no real provider. Local Docker with stand-in GitHub, Hetzner and SSH: given only the symptom, a new conversation diagnosed the failed administrator setup from fresh logs, the deployed bootstrap and upstream source, and its correction passed a command check proving the owner's login and rejecting the defaults. Linux host containers with TLS MinIO standing in for S3: BookStack with MariaDB and Healthchecks on the managed PostgreSQL were each recovered from a stopped first deployment through Pi under the original approval, protected through their declared owners' dump procedures, backed up, and restore-tested by the product with the copy booted in isolation and the owner's content proven inside it by Pi's chosen command checks. Five product defects found by the proofs were fixed in the same pass. A third pass closed three review gaps with focused regressions: reconciliation resumes verification from receipts instead of replaying completed checks, a held command holds across successor operations until the owner decides, and declared writers set a capture's consistency boundary, with online dumps proven by restoration. |
| [BookStack audit, 11 September](2026-09-11-bookstack-audit.md) | Real-Pi lifecycle trial of main `0dba3ec` with BookStack and a separate MariaDB, on local Docker with stand-ins. It found the investigation, verification, data-protection, input and UI gaps the follow-ups address. |
| [Native reuse, 11 September](https://github.com/lustoykov/hallvi/blob/0682ab257469bc5cee994572285283ea949bc3c6/docs/testing/2026-09-11-native-compose-reuse.md) | The second real-Pi install/update described above; local transport/provider stand-ins; no backup/rollback on that native topology. |
| [Generalized protection, 10 September](https://github.com/lustoykov/hallvi/blob/0682ab257469bc5cee994572285283ea949bc3c6/docs/testing/2026-09-10-generalized-protection.md) | Real local Paperless capture with PostgreSQL/files/broker, isolated full-stack restore, another processed document and interrupted-capture recovery. Transfer adapter was local, not R2/S3; useful full-app restore was harness evidence. |
| [Paperless intake, 10 September](https://github.com/lustoykov/hallvi/blob/0682ab257469bc5cee994572285283ea949bc3c6/docs/testing/2026-09-10-paperless-intake.md) | The more involved upstream stack/version trial and concrete intake gaps; separate from subsequent native Pi evidence. |
| [Compatible rollback](https://github.com/lustoykov/hallvi/blob/0682ab257469bc5cee994572285283ea949bc3c6/docs/architecture/compatible-rollback.md) | Scripted local Docker updates and return to retained verified images for SQLite and independent-build/shared-volume layouts; retained data, missing-image refusal and reconciliation. Not a full remote Pi rollback or proof that migrations are reversible. |
| [Scheduled protection, 9 September](https://github.com/lustoykov/hallvi/blob/0682ab257469bc5cee994572285283ea949bc3c6/docs/testing/2026-09-09-scheduled-backups.md) | R2 schedules/retention and restore evidence for PostgreSQL, Kuma and Grafana/Prometheus, with failure/interruption limits. Does not certify arbitrary native service names. |
| [Recovered controller worker, 9 September](https://github.com/lustoykov/hallvi/blob/0682ab257469bc5cee994572285283ea949bc3c6/docs/testing/2026-09-09-controller-worker-recovery.md) | Isolated Linux recovery followed by a real Pi request and clean rollback; not second-device recovery or complete independent takeover. |
| [Runtime, 9 September](https://github.com/lustoykov/hallvi/blob/0682ab257469bc5cee994572285283ea949bc3c6/docs/testing/2026-09-09-single-instance-runtime.md) | Live test-stack deployment/recreation and the disposition of provider/verification uncertainty. Later native architecture changes need their own evidence. |
| [Initial deployment, 8 September](https://github.com/lustoykov/hallvi/blob/0682ab257469bc5cee994572285283ea949bc3c6/docs/testing/2026-09-08-real-deployment-acceptance.md) | Product deployed a source app with private PostgreSQL on Hetzner and checked create/read/delete behavior. |
| [Integration acceptance, 9 September](https://github.com/lustoykov/hallvi/blob/0682ab257469bc5cee994572285283ea949bc3c6/docs/testing/2026-09-09-final-integration.md) | Conversation/view state, browser and operation-coordination checks, with explicit retained/retired legacy acceptance. Not current browser coverage for later changes. |

## Existing implementation proof gaps

These gaps describe the existing implementation and evidence, not a delivery checklist for the redesign. Background-only (no-HTTP) intake and on-demand re-verification, native backup/restore and rollback on real provider hosts, BYOM, additional public endpoints, replacement-host cutover and broader monitoring remain incomplete. Do not convert a requirements table into support claims. The historical [phase casebook](https://github.com/lustoykov/hallvi/blob/0682ab257469bc5cee994572285283ea949bc3c6/docs/archive/implementation/phase-one-acceptance.md) retains old rubrics and judgment evidence where legacy behavior still matters; it does not require restoring phase navigation.

Run checks proportionate to changed behavior. Distinguish scripted model callbacks, actual Pi, local Docker, simulated providers and real hosts. Preserve exact candidate/configuration, failed or uncertain outcomes, observation time and cleanup evidence. Do not repeat expensive model or full-suite runs without a concrete reason, and do not claim document/link checks validate runtime behavior.
